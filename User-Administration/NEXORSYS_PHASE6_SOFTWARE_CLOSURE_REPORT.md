# NexorSys Identity + Kiosk — Phase 6 Software Closure Report

**Assessment: NOT READY for external validation or commercial release.** Phase 6 closes several repository-solvable engineering gaps and adds verification, but does not establish a complete authorization matrix, provenance from a clean clone of the authoritative project commit, production provisioning, or commercial signing.

- Removed the tracked SQL backup archive and tracked root/scratch test artifacts from the current source tree after preserving the locally modified copies in a temporary quarantine directory. These files contained historical operational logs, badge identifiers, customer names and internal addresses; they are now ignored by `.gitignore` and cannot enter future release packages through the normal tree. This removes active copies from the current repository state, but does not erase their historical Git objects; the full-history Gitleaks result remains 21 redacted findings and requires owner-led credential rotation/revocation and any approved history remediation.
- The integrated Release regression was rerun after the cleanup: 186 tests passed, 0 failed, 0 skipped.
- Removed frontend debug/error-object console logging so Axios responses and error objects cannot be copied into browser logs; UI failure state remains the user-visible channel. Frontend tests passed 9/9, production build passed, largest JavaScript chunk is 283.31 kB, and a source scan found no `console.log`, `console.debug`, `console.warn`, or `console.error` calls.
- Hardened backend authentication and directory-search logging: local authentication now records only a username digest, failed local authentication stores only the digest, and user search terms/departments are omitted from exception logs. The full Release suite passed again with 186/186 tests and no skipped tests.
- Fixed the release-script bootstrap gap: `scripts/build-release.ps1` now performs its own solution restore before EF migration generation and publish. A fresh isolated checkout with no `obj/project.assets.json` successfully ran the script and produced an unsigned package (`sourceTreeClean:true`, `signed:false`, `commercialRelease:false`) with SHA-256 `C6FDB335FFF46176474B6CCABC7821685A0B4622E8C9636B8816A23C557D3A8E` from snapshot `15f5ade42fbe`.
- Current filtered snapshot `97c69260b5d6` passed the package build after these backend logging and MSI-inventory corrections. Two independent engineering package builds produced the same SHA-256 `4E35F62F0AA41A443D7349442D6B9E08F40D6D2B30B247803A3D5E5494BEFB64`; the artifact remains explicitly unsigned and non-commercial. The package build was run after a clean NuGet restore in the isolated snapshot; the full verifier evidence for the preceding source-equivalent state remains recorded below.
- Kiosk authentication responses no longer serialize the configured local executable path in `ManagedApp.Path`; the Agent-signed launch descriptor remains the only path used for process creation. The full Release regression passed again with 186 tests and no skipped tests.
- System backup failures no longer return raw PowerShell stdout/stderr to the API caller; the response is generic and logs retain only the exit code. Release solution build passed with `-warnaserror` after this change.
- Removed two unreferenced diagnostic programs (`backend/src/DbCheck.cs` and `backend/src/AuditCheck.cs`) that embedded a database password placeholder and printed user, badge and audit values. The locally modified copies were preserved in the recoverable quarantine; the files are no longer tracked or included in the source tree. Documentation connection examples now use secret-provider placeholders/null values.
- `build-release.ps1` was validated from a fresh checkout without a pre-existing restore, producing snapshot `15f5ade42fbe` and SHA-256 `C6FDB335FFF46176474B6CCABC7821685A0B4622E8C9636B8816A23C557D3A8E`. The subsequent source changes are covered by the latest full regression; a new package must be generated when the final report-only updates are included.
- Removed the remaining PIN-reset UI Badge UID column and binding; the administrative list now exposes only date, user, reason and actions. Frontend tests/build/audit passed again (9/9, 283.31 kB largest JavaScript chunk, 0 production vulnerabilities).
- The latest filtered clean snapshot `aacfbc5c8f1f` (455 tracked files, with local secrets/settings, generated output, backups and scratch artifacts excluded) passed the complete clean-checkout verifier after the active-artifact and frontend logging cleanup: integrated PostgreSQL Release tests (186 total, 0 failed/skipped), strict Release builds, Kiosk build, frontend clean install/tests (9/9)/production build (283.31 kB largest JavaScript chunk), npm audit (0 vulnerabilities), route inventory (97 actions), and package manifest/artifact validation. Two independent `build-release.ps1` executions produced byte-identical unsigned engineering ZIPs with SHA-256 `E0464E0DB144B6E9E17B3B635F464A6FF7C1A952B2AF131EE64127B1263121CE`; both report `sourceTreeClean:true`, `signed:false`, `commercialRelease:false`. This remains a synthetic local snapshot and does not establish authoritative remote provenance, commercial signing, or external validation.

## Latest verification (2026-09-16)

- Closed the remaining frontend false-empty failure states in Monitoring, Federation, Policies, Windows Trust, Audit, device enrollment, and Kiosk device loading. API failures now remain visible with status-specific operator messaging (401/403/5xx/network) and preserve previously loaded data instead of replacing it with empty arrays or zero counters. Frontend tests passed 13/13 and the production build passed; this is a UI reliability correction and does not change the NOT READY release assessment.
- Rebuilt the unsigned engineering package after the frontend correction: 220 valid ZIP entries, 0 invalid paths, 0 tracked generated release outputs, checksums and SBOM present. SHA-256: `A1D59DFC04C0E0D3A462409553B4454842C5CBCD4069EFA944A218A671FCFD6A`. Manifest remains `sourceTreeClean:false`, `signed:false`, `commercialRelease:false`; this is not a commercial release artifact.

- Final local engineering package is rebuilt as `NexorSys-Identity-Kiosk-1.5.0-a965814d8d6f.zip`; the exact SHA-256 is emitted with the delivered artifact. Archive audit reports 218 entries, 0 traversal paths, embedded checksums, 0 targeted host/customer residue files; manifest remains `sourceTreeClean:false`, `signed:false`, `commercialRelease:false` because this shared worktree is intentionally dirty and no commercial signing key is available.

- Removed the remaining tracked Windows build outputs (old MSI/WiX PDB/DLL/update ZIP) from the source tree after preserving them in recoverable quarantine. `.gitignore` now covers `build*` directories and generated binary/package extensions, CI rejects tracked generated outputs, and historical installer outputs cannot silently re-enter the repository. Maintained examples, CI, database utilities and JWT helper defaults now use NexorSys-neutral deployment values; the PostgreSQL connection utility was corrected to use `psql` rather than the unrelated SQL Server client. These changes do not remove intentional legacy-uninstall scripts that explicitly target old registry keys.

- Added a checked-out-tree mode to `scan-secrets-git.ps1` using the pinned Gitleaks binary and mandatory redaction. After removing the stale ignored frontend tree containing a private-network `.env.local`, the current worktree scan reports `no leaks found`; historical Git findings remain a separate owner-led rotation/history-remediation item.

- Re-ran `scripts/test-all.ps1 -Configuration Release`: 201 tests passed, 0 failed, 0 skipped; the Release build completed successfully. Demo/diagnostic scripts now use generic `.invalid` values or deployment environment variables, require explicit test credentials, and no longer embed customer SMTP/domain/credential defaults. All PowerShell scripts under `scripts/` now parse successfully.

- Hardened the administrative PIN utility: PIN input is hidden and mandatory, the BCrypt invocation is valid for the supported PowerShell runtime, and the audit record contains only a credential-updated marker; the PIN hash is never printed or persisted in audit data.

- Removed the last private-network and historical-user examples from maintained frontend placeholders; kiosk backend URL guidance is now deployment-neutral and login examples use a generic account name. Frontend tests remain 9/9.

- Removed the hard-coded development/customer origins from the API Content-Security-Policy. Production now permits only `self` plus explicitly configured HTTPS/WSS connect sources; HTTP/WS development origins are limited to loopback Development mode. Two regressions cover insecure-origin rejection and the absence of the former customer IP. The full Release regression remains green at 201 tests, 0 failed/skipped.

- Fixed a release-packaging defect where Windows URI-relative path normalization generated invalid `..//...` ZIP entries. `build-release.ps1` now uses filesystem-relative paths and refuses rooted or parent-traversal archive entries. The latest regenerated unsigned engineering package contains 218 valid entries, manifest, SBOM, and checksums; its SHA-256 is recorded with the delivered artifact.

- Removed three unreferenced customer-specific HTML documents from the frontend public assets and replaced the package README with a NexorSys commercial deployment README. Settings no longer contain customer URLs/examples or generate secret-shaped fake API keys; unsupported NFC/CPS/FIDO2/PSI simulation flags default to disabled and deployment endpoints remain unconfigured until provisioned. Final package content scan found no former customer IP/DNS/name residue, no invalid archive paths, and no embedded private-key/credential patterns.

- Removed the remaining development-host defaults from the distributed runtime: API `AllowedHosts` is deployment-provided rather than `localhost`, and the browser API client uses the serving origin rather than port `5000`. The dormant hard-coded HR bridge and tenant marker were removed. Frontend tests remain 9/9, strict Release build remains 0 warnings/errors, and the final package scan reports zero targeted host/customer residue.

- Replaced browser-side `Math.random()` generation for new-user PINs, passwords, and local application identifiers with `crypto.getRandomValues`; PIN/password generation uses unbiased rejection sampling. Frontend tests remain 9/9 and no `Math.random()` call remains in maintained frontend source.

- Closed the audit-service fail-open gap: `AuditService.Add` now refuses to stage any audit row when the organization context is missing or empty, instead of persisting `OrganizationId = Guid.Empty`. It also rejects an explicit or claim-derived user identity that belongs to another organization, including users already tracked in the current unit of work; missing synthetic identities remain subject to the production database foreign key. `AgentTrustController` now applies the same ownership check before writing actor IDs to Windows identity bindings or audit rows. Dedicated regressions cover both services. Full Release regression: 199 tests, 0 failed/skipped.

- Extended claim-derived actor ownership enforcement to direct audit factories in `UsersController`, `PolicyController`, `WorkflowController`, and `FleetController`, and to `GrantedBy`/`CreatedBy` fields during local-user provisioning. A shared validator detects foreign users even when system-admin query filters are bypassed, while preserving deliberately anonymous technical audit events and allowing quota-controlled technical provisioning to proceed without an HTTP actor. The integrated Release regression remains green at 199 tests, 0 failed/skipped.

- Applied the same tenant ownership validation to PIN-reset decision actors before persisting `ApprovedBy`; foreign actors are rejected while technical contexts without a persisted actor retain nullable attribution. The integrated Release regression remains green at 199 tests, 0 failed/skipped.

- Closed the registered generic workflow repository boundary: `GenericRepository<T>` now requires an organization context, explicitly scopes `GetById`/`GetAll`/`Delete`, and rejects foreign-tenant `Add`/`Update` entities. A `SYSTEM_ADMIN` A→B regression verifies the key lookup and mutation guard. Full Release regression: 196 tests, 0 failed/skipped.

- Closed the final federation-service read omission: provider health checks now require the explicit organization predicate like all other provider operations; the A→B regression proves a foreign provider is reported as unavailable rather than inspected or mutated. Full Release regression: 195 tests, 0 failed/skipped.

- Closed the infrastructure repository boundary gap: `UserRepository` now derives the current organization from `AppDbContext` and explicitly scopes every read/search/count/delete; add/update operations reject a mismatched organization even when global EF filters are bypassed. A `SYSTEM_ADMIN` A→B regression covers reads, counts, and foreign writes. Full Release regression: 195 tests, 0 failed/skipped.

- Closed tenant-boundary gaps in the registered NFC provider and authentication resolver: badge/PIN authentication and profile lookup now require the explicit organization context, and resolver-generated authentication events persist the verified tenant plus successful subject instead of defaulting to `Guid.Empty`. A `SYSTEM_ADMIN` A→B regression proves a foreign badge cannot authenticate; full Release regression: 194 tests, 0 failed/skipped.

- Closed tenant-boundary gaps in the registered-but-not-routed federation service: provider, migration-phase, authentication-policy, and federation-token reads/writes now require the explicit organization context; policy provider references and federation token owners are validated against that tenant even under query-filter bypass. A `SYSTEM_ADMIN` A→B regression proves foreign providers are not listed or mutated and foreign users cannot receive federation tokens. Full Release regression: 193 tests, 0 failed/skipped.

- Closed the remaining ANS audit-service tenant-boundary gap: `RecordDelegationAuditAsync` now requires an explicit organization context and verifies both the delegation owner and optional audit actor against that organization even when EF query filters are bypassed. An A→B `SYSTEM_ADMIN` regression proves foreign owner/actor inputs are rejected without creating an audit row. Full Release regression: 192 tests, 0 failed/skipped.

- Removed the remaining clear-text user identifier from inactivity-revocation logs: the background worker now logs a truncated SHA-256 digest of `SamAccountName` while retaining tenant-bound audit ownership and the exact user resource ID in the database audit row. Worker regression and the full Release suite pass; total is 191 tests, 0 failed/skipped.

- Hardened `AuditRedactor` for JSON-shaped snapshots in addition to key/value text. Password, token, credential and key properties are now redacted even when quoted JSON values are supplied; a regression confirms sensitive values disappear while safe fields remain. Full Release regression: 191 tests, 0 failed/skipped.

- Hardened `LicenseEntitlementGuard`: advancing the verified revocation sequence now updates only the license cache row with a relational `ExecuteUpdateAsync`; entitlement checks no longer call `SaveChangesAsync` and cannot flush unrelated tracked mutations. A regression proves pending user/audit changes are not saved by a feature check. Full Release regression: 190 tests, 0 failed/skipped.

- Closed a PIN-reset reason persistence gap: request reasons are now trimmed, bounded to 256 characters, and redacted before being stored in `PinResetRequest`. A dedicated regression proves password-shaped input is not persisted in clear text; the full Release regression passed with 190 tests, 0 failed and 0 skipped.

- Closed a sensitive-reason persistence gap in Windows authentication revocation: operator-supplied `Reason` values are now trimmed, bounded to 256 characters, and redacted before entering `AuthenticationEvent`. The regression proves a password-shaped value is stored only as `[REDACTED]`; the full Release suite remains at 189 tests with no failures or skips.

- Closed a local-user creation atomicity gap: `CreateLocalUser` no longer calls the legacy repository method that saves immediately. User, permissions, MIE devices, optional PIN, and audit are now staged in the same transaction/save. The dedicated regression confirms one combined mutation/audit save, and the full Release regression passed with 189 tests, 0 failed/skipped.

- Full `scripts/test-all.ps1 -Configuration Release` regression passed after the Vault entitlement hardening: 189 tests total (174 Security, 4 Windows Agent, 11 Kiosk), 0 failed and 0 skipped. The final engineering package above was generated from this same application source state; the report-only updates are intentionally not treated as a new commercial artifact.

- Closed a missing license boundary on Vault authorization: `POST /api/vault/{entryId}/authorize-use` now requires the signed `identity` entitlement before invoking `IVaultService`, while still deriving user and organization only from claims. Added a regression proving an unlicensed request returns 403 with zero service calls; the two Vault authorization tests pass. Agent Vault grant/redeem remains hard-disabled with `503 RELEASE_NOT_CONFIGURED`.

- Exact current-source filtered snapshot `b5ab63973a69` (455 tracked files; local secrets/settings, generated output, backups and scratch artifacts excluded) passed the complete clean-checkout verifier after the PIN-reset UI cleanup: integrated PostgreSQL Release tests (186 total, 0 failed/skipped), strict Release builds, Kiosk build, frontend clean install/tests (9/9)/production build (283.31 kB largest JavaScript chunk), npm audit (0 vulnerabilities), route inventory (97 actions), and package manifest/artifact validation. Two independent package builds were byte-identical, both SHA-256 `962C6F40CD71DBC07B0B1587EA26919700F029694637E585EE690FE49DECA185`; metadata confirms `sourceTreeClean:true`, `signed:false`, `commercialRelease:false`. This is a synthetic local snapshot, not authoritative remote-clone provenance, commercial signing, or external validation.

- Removed raw badge UID from `GET /api/Kiosk/status/{userId}`. The service now returns `HasBadge` plus device type/status, and the Kiosk UI renders enrollment state rather than the credential; the revoke-confirmation modal and dormant HR event bridge no longer send/display the raw UID. A controller-level regression seeds a known badge secret and asserts the status response includes only the boolean enrollment flag and neither the `BadgeUid` property nor secret value. Targeted regression passed 1/1; frontend tests passed 9/9, production build passed (largest JS chunk 283.45 kB), and `npm audit --omit=dev --audit-level=high` found 0 vulnerabilities. Integrated Release suite subsequently passed 183 tests (168 security, 4 Windows Agent, 11 Kiosk), 0 failures/skips; strict Release solution build passed with 0 warnings/errors and route inventory check confirmed all 97 entries current. A filtered isolated snapshot of current source (`59b2883`, 436 files; local secrets/settings, backups, scratch/presentation and generated directories excluded) then passed the full clean-checkout verification pipeline: restore/build, PostgreSQL tests (183 total, 0 failed/skipped), Kiosk Release build, frontend clean install/tests (9/9)/production build (283.45 kB largest JS), npm audit (0 vulnerabilities), route inventory generation (97 actions), unsigned package creation and manifest/artifact validation. ZIP SHA-256 `D870464F89BEB1D30A0DF1DF8463225836C0B59365041307A8F5B6BFE6D2DE7C`; manifest confirms clean synthetic tree, unsigned, `commercialRelease:false`. This is not a fresh clone of an authoritative remote commit, not an independent two-build reproducibility comparison, and not commercial signing proof.
- Closed a malformed-owner gap in PIN reset administration: pending-request projections remain tenant-scoped and render a cross-tenant/missing owner only as `Unknown`, while approve/reject now require the referenced user to belong to the request tenant before any terminal update or audit. The new filter-bypass regression proves the malformed request remains pending and creates no event. Integrated Release suite after this change passed 184 tests (169 security, 4 Windows Agent, 11 Kiosk), 0 failures/skips. The earlier isolated package snapshot predates this change and is not evidence for this exact source state.
- Removed the obsolete frontend ANS “Identifiant MIE” column and binding; the API never exposes that credential field, and the UI no longer retains a latent rendering path for it. Frontend verification after the change passed 9/9 tests and the production build passed with a 283.45 kB largest JavaScript chunk.
- Current-source filtered snapshot `12ae31b` (436 files; local secrets/settings, backups, scratch/presentation and generated directories excluded) passed the complete clean-checkout verification after the PIN-reset ownership and ANS UI corrections: restore, strict Release builds (0 warnings/errors), integrated PostgreSQL suite (169 security + 4 Windows Agent + 11 Kiosk = 184, 0 failed/skipped), Kiosk Release build, frontend clean install/tests (9/9)/production build (283.45 kB largest JS), npm audit (0 vulnerabilities), route inventory generation (97 actions), and unsigned package manifest/artifact validation. ZIP SHA-256 `CC6091A66D293400BACE92DB97E0E67565043CE2218D9DFB576B40A2DDE828C6`; manifest confirms `sourceTreeClean:true`, `signed:false`, `commercialRelease:false`. This remains a synthetic filtered snapshot, not authoritative remote-clone provenance, commercial signing, or external validation.
- Two independent `build-release.ps1` executions from snapshot `12ae31b` produced byte-identical ZIP artifacts, both with SHA-256 `CC6091A66D293400BACE92DB97E0E67565043CE2218D9DFB576B40A2DDE828C6`. This establishes local package reproducibility for that filtered snapshot only; it does not establish canonical Git provenance or commercial signing.
- A current full-worktree Gitleaks scan reported four `generic-api-key` matches, all redacted placeholder detections: two explanatory `REDACTED` strings in documentation and two occurrences in a pre-existing ignored bundle under the separate `frontend/nexorsys-identity-ui/dist` tree. No secret bytes were exposed by the scan. Historical Git findings remain unresolved potential exposures and still require owner confirmation, rotation, and any approved history remediation; this is not a clean-history result.
- The secret-scan helper now also supports an explicit `-AllHistory` mode that scans every reachable Git ref with redacted output; PowerShell parsing and `git diff --check` pass. Running it locally scanned 44 commits and reported 21 redacted historical findings (generic API-key/JWT rules across prior settings, test and scratch files). This confirms the historical blocker rather than clearing it: owners must determine whether any value was live, rotate/revoke affected credentials and sessions, audit clones/forks/CI artifacts, and coordinate any approved history rewrite. No credential rotation or history rewrite was performed.
- The secret-scan workflow now runs the historical mode on manual dispatch and weekly schedule, while retaining the incoming-range gate for pull requests and pushes. Both modes keep scanner output redacted; scheduled CI is an ongoing detection control, not credential rotation or history rewriting.
- Added an automated route-boundary regression: every reflected HTTP action must have `[Authorize]` or an approved custom authentication filter, and anonymous actions are allowed only for the login, certificate-possession proofs, or explicitly protected Agent/Kiosk/Fleet filter surfaces. The guard passed 1/1 targeted and the subsequent Release suite passed 185 tests (170 security, 4 Windows Agent, 11 Kiosk), 0 failures/skips.
- Current-source filtered snapshot `6c2ddec` (436 files) passed clean-checkout verification after the route-boundary regression: restore, Release builds with 0 warnings/errors, integrated PostgreSQL tests (185 total, 0 failed/skipped), Kiosk Release build, frontend clean install/tests (9/9)/production build (283.45 kB largest JS), npm audit (0 vulnerabilities), 97-route inventory, and unsigned artifact validation. ZIP SHA-256 `1DC7014B7CCB329374BC8AFEB807D23BFBF43BC6AE201F79C36D04383D9109D8`; manifest confirms clean synthetic tree, unsigned, non-commercial. This is not authoritative remote provenance or external validation.
- Removed the legacy tracked `kiosk-windows-auth/NexorsysKioskAuth.msi` binary (approximately 51 MiB) from the release source tree and added a targeted `.gitignore` rule for future generated MSI files. WiX sources and `build-msi.ps1` remain the authoritative build inputs; commercial MSI signing remains unavailable without the authorized signing certificate/timestamp infrastructure.
- Follow-up inventory found two additional tracked generated MSI binaries (`NexorsysKioskAuth_x64.msi` and `Setup.msi`, approximately 118 MiB combined). They were moved to the existing recoverable local quarantine and removed from the index; `git ls-files '*.msi'` now returns zero tracked MSI files. The package builder already excludes MSI inputs, but this closes the repository inventory gap; no signed MSI was produced.
- Current-source filtered snapshot `a33647c` (435 files; legacy MSI absent) passed the complete clean-checkout pipeline: restore, Release builds with 0 warnings/errors, integrated PostgreSQL suite (185 tests, 0 failed/skipped), Kiosk Release build, frontend clean install/tests (9/9)/production build (283.45 kB largest JS), npm audit (0 vulnerabilities), 97-route inventory, and unsigned artifact validation. ZIP SHA-256 `CFA70472D602E6000638A3DD486AEDB948E19ACC7CD773E1765D3E52C0764C9E`; manifest confirms clean synthetic tree, unsigned, non-commercial. This does not prove canonical remote provenance, signing, or external validation.
- Release safety check: invoking `build-release.ps1` against the dirty primary worktree was rejected before packaging with `Release packaging requires a clean Git worktree`; the engineering override remains explicit and separate. `build-msi.ps1` likewise requires CMake/WiX and only signs when a valid code-signing certificate, HTTPS RFC3161 timestamp, `signtool.exe`, and post-sign verification are all present.
- CI now invokes `scripts/verify-clean-checkout.ps1` on the clean Linux checkout and uploads only the resulting unsigned engineering ZIP as a build artifact. The verification includes the disposable PostgreSQL regression suite, strict Release builds, frontend tests/build/audit, route-inventory drift checking, package manifest/checksum validation, and the explicit `commercialRelease:false`/`signed:false` assertions. This closes the repository-level CI wiring gap; it does not provide commercial signing, canonical release provenance, or external validation.
- Added server-generated audit correlation IDs at the `AppDbContext` save boundary, covering both `IAuditService` and direct `AuditLog` inserts. Correlation uses the current W3C activity trace when available and a server-generated fallback otherwise; caller-supplied correlation values are overwritten. The audit API exposes the correlation ID for authorized tenant-scoped log retrieval. PostgreSQL migrations and the regression test passed; the integrated Release suite subsequently passed 171 security tests plus 4 Windows Agent and 11 Kiosk tests (186 total), with 0 failures/skips.
- Current-source filtered snapshot `7a721580beae` (455 files; local secrets/settings, backups, scratch, generated/dependency directories, and legacy MSI excluded) passed the complete clean-checkout verification including the audit-correlation migration: strict Release build, integrated PostgreSQL suite (171 security + 4 Windows Agent + 11 Kiosk = 186, 0 failed/skipped), Kiosk Release build, frontend clean install/tests (9/9)/production build (283.45 kB largest JS), npm audit (0 vulnerabilities), 97-route inventory, and package manifest/checksum validation. ZIP SHA-256 `13076C212F0C6E2A9623C9C89B9CF32EE532D241C09FBA36FA519BC7E785E379`; an independent second `build-release.ps1` execution from the same clean snapshot produced the identical SHA-256. The artifact is unsigned with `commercialRelease:false`; this proves local snapshot reproducibility only, not canonical remote provenance, commercial signing, or external validation.
- Added controller-level negative integration coverage for seven route actions previously missing test-name references: federation provider/callback stubs, Kiosk welcome-session, Windows session-created, and system restore/reset. All deliberately return 501 and tests prove the seeded user/session/audit state is unchanged. Targeted test passed 1/1. A simple action-name scan now finds all 97 inventory action names somewhere in test source; this is only a heuristic and is not proof that each action has a meaningful authorization/ownership test. The latest clean synthetic snapshot below includes this regression and passes the full clean-checkout/package reproducibility pipeline. Latest integrated Release verification passed 182 tests (167 security, 4 Windows Agent, 11 Kiosk), 0 failed/skipped; strict build passed with 0 warnings/errors, and the 97-action inventory check passed.
- Current-source filtered synthetic clean snapshot `3155c96d511f` (434 files copied from tracked/unignored worktree content; local `.env`/`custom_settings.json`, backups, scratch/presentation, and generated directories excluded; includes the A→B self-profile mutation and fail-closed disabled-route regressions) passed `scripts/verify-clean-checkout.ps1 -Configuration Release`: strict solution build (0 warnings/errors), integrated disposable-PostgreSQL suite (167 security + 4 Agent + 11 Kiosk = 182, 0 failed/skipped), Kiosk Release build, frontend clean install and tests (9/9), production build (largest JS 283.45 kB), npm production audit (0 vulnerabilities), generated 97-action route inventory, and package manifest/checksum/artifact validation. Two independent package builds from this exact clean synthetic commit were byte-identical, SHA-256 `970006CCA4E31F4762AFD956E653DFA9E76F629854343A3E95382A60C49B5486`; manifest confirms `sourceTreeClean:true`, `signed:false`, `commercialRelease:false`. The main checkout remains dirty and unmodified by snapshot construction. The report was updated after packaging; application/test source did not change. This establishes reproducibility for this filtered local snapshot only, not a fresh clone of the authoritative remote commit or commercial signing.
- Closed remaining Identity entitlement bypasses on `POST /api/auth/login`, `GET /api/auth/me`, `GET/PATCH /api/users/me`, and admin SignalR group membership. Login checks the signed `identity` entitlement before user lookup, local password verification, or LDAP bind; profile routes check before reads or password/profile changes; the hub derives organization from signed `org_id` and checks entitlement before automatic or explicit admin-group membership, aborting unlicensed admin connections. Logout intentionally remains available for session cleanup. Tests verify no LDAP/session, no profile/password/audit mutation, tenant-bound SignalR entitlement allow/deny/missing-context behavior, and A→B self-profile update denial with the foreign user and audit table unchanged. The focused Identity entitlement tests passed, including the A→B update regression. Latest integrated Release `scripts/test-all.ps1` passed 182 tests (167 security, 4 Windows Agent, 11 Kiosk), 0 failed/skipped, with disposable PostgreSQL; strict Release solution build passed with 0 warnings/errors. This does not close the exhaustive authorization matrix or establish commercial readiness.
- A filtered synthetic clean-source snapshot from the current worktree, excluding local `.env`/`custom_settings.json`, copied database backups, and generated build/dependency directories, was committed only in a temporary repository as `b8dbb3508d61`. `scripts/verify-clean-checkout.ps1 -Configuration Release` passed there: restore; strict solution build (0 warnings/errors); integrated PostgreSQL tests (162 security + 4 Windows Agent + 11 Kiosk, 177 total, 0 failures/skips); Kiosk Release build; frontend `npm ci`, 9/9 tests, production build (largest JS chunk 283.45 kB), production npm audit (0 vulnerabilities); release packaging and manifest/checksum verification. A second independent `build-release.ps1` run from the same clean synthetic commit reproduced the identical ZIP SHA-256 `406868A980C05AB5556F7340FBECFD5E9D6ED691EFF1E9A251A9D18245606EB3`; manifest records clean synthetic tree, `signed:false`, `commercialRelease:false`. Gitleaks source scan reported two generic-api-key false positives from documentation's explanatory use of “password(s)” (sensitive values redacted); generated dist and local settings were excluded. This proves reproducible packaging for a filtered synthetic snapshot, not a canonical remote clone, a committed customer source tree, or commercial signing.
- Closed a workstation-enrollment entitlement gap: create, detail, device/Agent certificate approval, and both certificate-proof transitions require the signed `kiosk` module; proof routes validate the presented certificate, resolve its exact tenant-bound workstation binding, and then check entitlement before the conditional lifecycle write. Added PostgreSQL controller-level proof tests using an injected test trust validator: both proof routes deny with identity-only licensing without mutation/audit, then succeed and write the exact terminal state/audits when the signed Kiosk module is enabled. This exercises API/SQL/license ordering but does not validate the host OS certificate chain or customer PKI. Revocation/decommission remain usable as safety operations. Integrated Release suite passed 177 tests (162 security including PostgreSQL, 4 Windows Agent, 11 Kiosk), 0 failures/skips; strict Release build passed with 0 warnings/errors, route inventory check reports 97 current actions, and `git diff --check` is clean.
- Closed the Fleet `kiosk` entitlement gap: fleet inventory reads and authenticated workstation heartbeats now check the signed module before data queries or state changes; disabling workstation trust remains available as an emergency safety operation. Added regression coverage asserting unlicensed inventory/heartbeat calls return 403 before loading/updating fleet rows and create no audit. Targeted test passed; integrated Release suite passed 175 tests (160 security, 4 Windows Agent, 11 Kiosk), 0 failures/skips; strict solution build passed with 0 warnings/errors.
- Production readiness now checks the currently verified signed revocation snapshot in addition to database connectivity/schema. Missing, corrupt, or expired snapshot returns `503 NotReady` with a non-sensitive licensing status and a structured error log; the entitlement guard already denied licensed operations, but readiness previously still reported healthy after snapshot expiry. Added a PostgreSQL-backed controlled-clock regression proving readiness succeeds before expiry and fails once expired. Full integrated Release suite passed: 159 security tests (including PostgreSQL), 4 Windows Agent, 11 Kiosk; 174 total, 0 failures/skips. Strict Release solution build also passed with `-warnaserror`, 0 warnings/errors.
- Strengthened the licensed-quota race regression to invoke the actual `UsersController.CreateLocalUser` and `WorkstationEnrollmentController.Create` endpoints concurrently against a signed one-user/one-workstation license. Each pair uses separate DbContexts and distinct resources; the test requires each response to be either a successful creation or a quota conflict, exactly one success per resource type, and a final persisted count of one. Integrated Release suite passed after the change: 158 security tests (including PostgreSQL), 4 Windows Agent, 11 Kiosk; 173 total, 0 failures/skips. Route inventory check remains current at 97 actions; `git diff --check` is clean for the edited test/report.
- Workflow review decisions are one-way: only `pending` can transition to `approved` or `rejected`; repeat decisions return 409 `WORKFLOW_ALREADY_DECIDED`. Relational providers perform a conditional pending-only SQL update in a transaction with the single audit insert. A PostgreSQL race test now submits competing approve/reject actions and verifies exactly one succeeds, the other receives the conflict, and exactly one matching audit commits. The repeated-decision state/audit regression and full integrated Release suite passed: 158 security tests (including PostgreSQL), 4 Windows Agent, 11 Kiosk; 173 total, 0 failures/skips.
- Added `CredentialOwnershipGuard` to user creation/update, device registration, and device reactivation. It canonicalizes credential text and checks user badge/CPS/FIDO fields, device identifiers/serials, and PIN bindings across tenants with query filters bypassed; conflicts reveal no foreign owner and are returned before mutation/audit. Added normalized collision/no-partial-write tests plus a PostgreSQL integration test over all credential sources. Full Release run passed: strict solution build (0 warnings/errors) and 172 .NET tests (157 security, 4 Windows Agent, 11 Kiosk), 0 failed/skipped; the PostgreSQL guard translation/integration test passed.
- The isolated synthetic clean snapshot `7a3b5fe006c1` (including credential ownership protection) passed `scripts/verify-clean-checkout.ps1`: strict Release solution build, 172 .NET tests (157 security, 4 Windows Agent, 11 Kiosk; zero failures/skips), Kiosk Release build, frontend clean install/tests (9/9)/production build (largest chunk 283.45 kB), npm production audit (0 vulnerabilities), current 97-action inventory, and package manifest/checksum/artifact scans. Engineering ZIP SHA-256 `36F0C294A4C0737D2467A6BB149ABBE97F35EA9BD3097AB9D1971232F8DEF475`; manifest says clean synthetic source tree, unsigned, `commercialRelease: false`. This is not a canonical remote clone or commercial signature proof. The report update follows packaging; application and test source are unchanged.
- `UsersController.UpdateUser` now distinguishes HR/Director directory editing from administrative security changes. HR/Director can update descriptive fields, but a request containing any protected field, badge/CPS/FIDO identifier, RPPS/MIE type, account-active state, PIN-change policy, app permissions, or role change is rejected as a whole unless the actor has an `AdminOnly` role; SuperAdmin-only role escalation remains separately enforced. Regression proves denial leaves email, badge, active state and permissions unchanged while descriptive edits still succeed. Full Release verification after this change passed: strict solution build with 0 warnings/errors and 169 .NET tests (154 security, 4 Windows Agent, 11 Kiosk), 0 failed/skipped.
- Isolated synthetic snapshot `17f4bbabaa5d`, including the user-update role boundary, passed the full `scripts/verify-clean-checkout.ps1` pipeline: strict Release build (0 warnings/errors), all 169 .NET tests (154 security, 4 Agent, 11 Kiosk; 0 failures/skips), Kiosk Release build, frontend 9/9, production build (283.45 kB largest JS chunk), npm audit (0 vulnerabilities), current 97-action route inventory, and package metadata/checksum/artifact scans. Unsigned engineering ZIP SHA-256 `E2CDA932CDD9D4CEB3B64DB2598D8C064484FAD6747DD6B635A28806767A13E3`; manifest says clean synthetic source tree, unsigned, `commercialRelease: false`. This is one clean synthetic build, not canonical remote provenance or proof of production signing.
- Integrated local Release regression passed 168 tests (153 security, 4 Windows Agent, 11 Kiosk), with 0 failures/skips; strict solution build passed with 0 warnings/errors. Device A→B coverage is included. The generated route/action inventory remains at 97 entries; it is not an exhaustive resource-level authorization proof.
- Removed the unused `IKioskService.RegisterAppOpeningAsync` contract and its implementation, which unconditionally returned success without validating or recording an app opening. No production call site existed; application sessions use the separately authorized Kiosk/Agent flow. Rebuilt the strict Release solution and reran the integrated suite after removal: 168 passed (153 security, 4 Windows Agent, 11 Kiosk), 0 failed/skipped. The subsequent isolated clean-snapshot verification is recorded below.
- Post-removal isolated synthetic snapshot `fa897fe601eb` passed the full `scripts/verify-clean-checkout.ps1` pipeline: strict Release solution build (0 warnings/errors), 168 .NET tests (153 security, 4 Windows Agent, 11 Kiosk; 0 failures/skips), Kiosk Release build, frontend clean install/tests (9/9)/production build (largest chunk 283.45 kB), npm production audit (0 vulnerabilities), generated 97-action inventory, and package manifest/checksum/artifact scans. Unsigned engineering ZIP SHA-256: `E363E558954668C7C966C3FF8B15DAB35BF5D81F3F1E95AC417A9B832541BE7E`; metadata reports clean synthetic source tree, unsigned, `commercialRelease: false`. This was a single package build from an isolated synthetic local commit, not an independent reproducibility pair or canonical remote clone. The report update follows packaging; application and test source did not change afterward.
- Isolated synthetic clean snapshot `2363ee459913` passed `scripts/verify-clean-checkout.ps1`: strict Release solution build, all 168 .NET tests, Kiosk Release build, frontend clean install and tests (9/9), production frontend build (largest JS chunk 283.45 kB), production dependency audit (0 vulnerabilities), 97 route/action entries, and packaging metadata/checksum/artifact scans. The unsigned engineering ZIP `NexorSys-Identity-Kiosk-1.5.0-2363ee459913.zip` has SHA-256 `5D352EDEA1C5BD00C5565B83BC14F93E59C50215155078DFE93370F868E91C99`; metadata confirms a clean synthetic source tree, no signature, and `commercialRelease: false`. A second independent reproduction of this exact snapshot has not yet been run. This synthetic commit does not establish canonical remote provenance. The report update itself follows package generation; application and test source are unchanged.

## Closed or materially advanced in this repository

- Added a global ASP.NET Core fallback authorization policy requiring an authenticated principal for endpoints without explicit authorization metadata. Public routes remain explicitly `[AllowAnonymous]`; Agent routes marked anonymous to JWT remain gated by the mandatory mTLS controller action filter. The generated route inventory now records the effective `fallback-authenticated` boundary and declares the default policy, preventing omitted attributes from being mislabeled as open routes. A policy-registration test verifies the fallback and retained role policy.
- Added a generated inventory of routeable controller actions, HTTP routes, framework anonymous metadata, and declared authentication filters (`docs/api-route-inventory.json`; now 97 entries). The generator now excludes `[NonAction]` methods even when they retain legacy HTTP attributes; five retired Windows-auth methods had previously been misreported as routes. It distinguishes Agent routes marked anonymous to JWT from the required controller-level Agent mTLS action filter. This inventory is not proof of resource-level authorization.
- Added a generator `--check` mode that compares the checked-in route inventory against the compiled API assembly while ignoring only `GeneratedAtUtc`; CI now fails if a route, verb, authorization attribute, or custom authentication boundary changes without regenerating the inventory. Local verification confirms 97 current route/action entries. This prevents inventory drift but does not replace per-resource A→B tests.
- Added PostgreSQL-backed concurrent quota tests for user and workstation limits, revocation snapshot persistence/monotonicity tests, and a Data Protection key-ring restart/certificate test. Revocation sequence rollback is now rejected even when the durable signed snapshot has expired: the cache verifies the old signature and preserves its sequence floor without treating the expired snapshot as valid for licensing. A controlled-clock test verifies expired-cache rollback denial and acceptance of a higher sequence.
- Quota-backed user and workstation creation/synchronization transactions now translate PostgreSQL serialization failures (`40001`) into `409 CONCURRENT_WRITE_RETRY` rather than leaking an unhandled server error (local user creation previously collapsed it to 500; bulk directory sync to 503). Other SQL states retain their existing error handling. Added focused tests for nested serialization-error detection and rejection of unrelated SQL states.
- Closed a missed entitlement boundary on ANS delegation-log reads: the endpoint exposed tenant-scoped RPPS/delegation history to HR/Admin users without checking the signed `identity` module. It now denies before querying when that module is inactive; a test asserts 403 and that no delegation entity was materialized. Updated the authorization matrix. The actual pairing route remains an explicit no-op 501 until its external healthcare/badge proof integration is available.
- Closed a missed entitlement boundary on the generic Workflow controller: list/read/create/status were tenant-scoped but bypassed signed feature licensing. All four now require the `identity` module before data access; regression coverage asserts 403, no tracked workflow read, no status mutation, and no audit insert. Updated the route-family authorization matrix; Kiosk-specific workflow routes retain their separate `kiosk` entitlement.
- Closed the same entitlement gap on the administrative audit and monitoring read surfaces. Audit history, sign-in event details, and workstation/user statistics now require signed `identity` before querying; no-license tests exercise audit list, monitor stats and recent events and assert denial without sensitive response payload or tracked data. Added explicit matrix treatment. Authenticated health/readiness status remains available separately and returns only deployment readiness/schema state.
- Hardened the authenticated `/api/auth/me` read: it now requires matching signed user and organization claims in the SQL query and selects only the public profile fields, instead of loading the full user graph (including credential/device/grant entities) through `UserRepository`. Regression coverage uses a query-filter-bypass administrator context to prove a mismatched org claim is denied and checks that the response contains no password/PIN/device fields. The legacy Kiosk `/identify` alias now also has an explicit cross-tenant workstation-denial assertion, alongside `/identify-mie`.
- Added an A→B ownership regression for the Agent Windows-session end action, which previously had no direct test. With query filters bypassed, a caller authenticated as one workstation can revoke only the binding that simultaneously matches its tenant, workstation, certificate, SID, and Windows session ID; foreign-tenant/workstation and mismatched certificate/SID/session bindings stay active, and exactly the matched binding is audited. The authorization matrix now calls out this coverage.
- Added controller-level fail-closed regressions for both Agent Vault operations: grant and redemption return `503 RELEASE_NOT_CONFIGURED`, emit tenant-bound denial audits, and expose no plaintext. The unconfigured Vault release service is not invoked; Vault remains disabled.
- Fixed a concrete background ownership defect in automatic inactivity revocation: the worker had used request-scoped organization filters and an HTTP-scoped audit service, so with no request context it selected no users (and could not reliably stamp tenant-owned audits). It now explicitly queries across tenants, excludes unowned records, and persists each deactivation with an audit row carrying the exact organization and affected resource ID (with no false human actor) in the same save. A regression runs with no organization context and verifies two tenants, recent/unowned exclusions, and tenant-bound audit ownership.
- Fixed the same absent-HTTP-tenant failure in data retention: it now explicitly bypasses tenant filters for the configured three-year AuthenticationEvent and one-year UserSession retention predicates. A PostgreSQL regression verifies expired rows are removed in both tenants while newer rows are retained. Updated the ownership matrix; Vault remains disabled.
- Closed a Data Protection key-ring validation asymmetry: Unix now rejects symbolic-link/reparse-point XML key entries and any group/other permissions on key files (while requiring owner-read access), matching the per-file protection already enforced on Windows. Added regressions for group-readable key files and symlink entries; the Unix-specific tests were executed under Linux in the .NET 8 SDK container, not inferred from the Windows run.
- Corrected schema/migration drift in Agent certificate identity: the EF model declared `agent_certificate_thumbprint` unique, but the migration-created PostgreSQL index was non-unique. Added a migration that preflights and clearly aborts on existing duplicate bindings, then enforces a filtered unique index, plus an integration regression that inspects the migrated PostgreSQL catalog. This prevents ambiguous certificate-to-workstation bindings; existing duplicate data requires explicit operator resolution and is never auto-selected.
- Added an A→B workstation-enrollment creation regression: the route now depends on the registered license-guard abstraction, the authenticated organization is forced onto the inserted row, the audit is invoked, and an existing foreign-tenant workstation remains unchanged.
- Bounded revocation-feed response reads before JSON deserialization. With `ResponseHeadersRead`, the prior code relied on Content-Length and checked the signed field only after parsing, allowing an unknown-length response to consume unbounded memory. The client now buffers no more than 65,536 bytes (plus one overflow-probe byte), rejects unknown JSON fields, and tests both the exact limit and oversized unknown-length stream.
- Added controller-level fail-closed coverage for both anonymous workstation certificate-proof routes: absent client certificates return 401 before workstation or audit reads/writes.
- Implemented one-time Kiosk application-session authorization through the named-pipe Agent and mTLS Identity API, including server-side ownership checks, expiry, replay protection, executable path/hash/publisher constraints, no command-line arguments, audit, and fail-closed session cleanup. Expanded automated Kiosk launch/trust coverage to include expired/malformed/untrusted grants, wrong-session grants, unavailable Agent, absent server session, process-launch rejection, and a controlled signed-executable success path.
- Closed a concrete IPC trust gap: Agent now allowlists the exact Kiosk executable path, Authenticode publisher, and SHA-256 observed from the caller process; Kiosk now pins all three for the Agent server process. Missing or malformed pins fail closed. Added unit coverage for path/publisher/hash/signature mismatches. Protected machine-registry provisioning and exact production binary pins are documented in `docs/phase5-security-operations.md`; customer installation, release-signing and pin-rotation rehearsal remain external deployment validation.
- Removed the Kiosk/Fleet API-key action filter's `[AllowAnonymous]` bypass. Those device surfaces have no public anonymous operation; any action-level anonymous metadata must not disable their production mTLS/device check. Added filter-level tests proving Production denies without a certificate even when a development API key or anonymous metadata is present, Development accepts only the configured key, and a mismatched key never invokes the action.
- Fixed the fallback-policy/device-auth integration: Kiosk device operations and Fleet heartbeat now carry explicit `[AllowAnonymous]` endpoint metadata so ASP.NET's authenticated-user fallback does not reject certificate-authenticated devices before MVC action filters run. The ApiKeyAuth filter still enforces mTLS in Production, and only passes explicitly `[Authorize]` endpoints when a principal is authenticated (authorization middleware has already enforced its policy); `[AllowAnonymous]` metadata never bypasses the filter. Admin routes remain JWT/role-protected. Regenerated the 97-action route inventory and added reflection/filter tests for both paths.
- Added a PostgreSQL-backed Agent controller test for application launch validation: foreign-tenant session, same-tenant wrong-user and wrong-workstation sessions, wrong Windows binding/session ID, unlicensed denial without consumption, audit-failure rollback, correct grant, replay rejection, returned executable identity/expiry, atomic one-time state, and authorization audit. This test exposed and fixed a production query translation failure: executable-identity policy validation now runs after the active application has been fetched under the Agent's tenant boundary, rather than inside an untranslatable SQL predicate.
- Made Agent launch authorization reads, entitlement/binding/session/application/permission checks, one-time Kiosk launch consumption, and its audit one PostgreSQL serializable transaction. This closes a stale-read window where a permission or session revocation could commit after checks but before the one-time consume transaction began. A PostgreSQL regression verifies that a pre-created application session with subsequently revoked application permission is denied, remains unconsumed, and is audited. A failure-injection trigger still proves an audit insert failure rolls back consumption; replay remains denied.
- Rechecks the signed `kiosk` feature entitlement at Agent launch authorization time, not only when the Kiosk originally creates the application session. An expired/revoked license or removed module therefore prevents a still-live session from obtaining a new process-start grant; the PostgreSQL controller test verifies denial does not consume the session.
- Also gates creation/refresh of Agent Windows-session bindings on the signed `kiosk` entitlement; session-end/revocation remains available without entitlement for cleanup. The PostgreSQL test verifies an unlicensed Agent cannot create a binding or obtain a launch grant.
- Requires the signed `kiosk` entitlement before Fleet admin can enable workstation trust; disabling trust remains available without an active license so operators can contain or recover a workstation. A regression test verifies an unlicensed enable attempt leaves both workstation state and audit unchanged.
- Gates new Agent trust configuration (Windows SID binding and executable publisher/hash/path registration) on the signed `kiosk` entitlement. Revocation remains available when unlicensed for containment. Endpoint tests prove denied configuration writes neither state nor audit.
- Hardened Kiosk credential service ownership for SYSTEM_ADMIN/global-filter-bypass contexts. PIN reset no longer renames another user's PIN to resolve collisions; badge assignment denies another tenant's existing user/PIN/device identity instead of taking it over. Unlock, revoke, reset, security-status, session-ending, and PIN-reset workflow operations now bind explicitly to the selected organization; newly created PIN, device, and workflow records persist that organization. In-memory A→B and PostgreSQL ownership tests cover foreign rows remaining unchanged and correct organization persistence.
- Kiosk PIN validation now returns success only if the subsequent server session was actually created with a nonempty token; a race/lifecycle failure is audited as denial and returns 401. The authorized-application projection also requires both the permission and referenced application to belong to the authenticated organization.
- Kiosk PIN validation now queries only globally matching normalized device identifiers when checking for cross-tenant duplicate credential ownership; it no longer materializes the full `UserDevices` table from every tenant for each PIN attempt. In-memory and PostgreSQL regressions verify a foreign duplicate still denies authentication, including SQL translation on PostgreSQL.
- Kiosk application-session stop now fetches the owner session only when tenant, workstation, and user IDs all match the application-session row. A SYSTEM_ADMIN query-filter-bypass regression seeds a Tenant A application session linked to a Tenant B user session and verifies it is denied without ending either session.
- Kiosk PIN lockout now updates the failed-attempt counter atomically in SQL and commits the counter plus audit in one transaction. A PostgreSQL race test sends 12 simultaneous invalid attempts and proves the counter reaches the threshold exactly once, the account is locked, and concurrent writes do not lose increments.
- Split the former shared/global fixed-window auth limiter into per-IP login (5 requests/minute) and Kiosk (100 requests/minute) policies while retaining the per-IP global ceiling. Regression coverage consumes each configured partition, verifies the exact limits, and proves distinct IPs receive distinct login partitions.
- Moved Agent certificate authentication into a mandatory controller-level action filter so all current and future routed Agent actions pass mTLS, active-workstation, and certificate-proof checks before action logic. Added fail-closed non-HTTPS filter coverage and a test that enumerates the full anonymous-route allowlist.
- Added multi-controller tenant A→B checks for device reads/creation/status changes, policy reads/update/delete and forged-tenant creation, audit visibility, workstation enrollment lookup, workflow reads/status changes, PIN-reset review, and direct suppression of Tenant B ANS delegation rows in the Tenant A log projection. These strengthen coverage but do not exhaust every route/resource pair.
- Expanded user-directory A→B coverage across every existing directory selector/action tested: list/search/statistics, read by ID and account name, update by ID and account name, and delete. A tenant administrator receives no foreign-user projection and cannot change the other tenant's email, activation state, or role.
- Extended Agent trust A→B coverage: a tenant administrator cannot register executable trust for another tenant's application or bind/revoke that tenant's Windows identity; own-tenant executable registration remains functional.
- Bound Auth logout to the active session ID, authenticated user ID, and organization ID. A regression test proves a Tenant A principal cannot close Tenant B's session and can still close its own.
- Changed login to reject an identity that is not provisioned in the selected organization before attempting LDAP authentication, eliminating the API's ability to act as a credential-validation oracle for unprovisioned directory accounts. The denial response is generic and a spy-based test proves LDAP is not called.
- Hardened system-backup password reauthentication to reload the account inside the signed organization and require its current active, unlocked `SYSTEM_ADMIN` state. Four tests cover inactive, locked, demoted, and foreign-organization accounts; each is rejected before process invocation.
- Expanded workstation lifecycle A→B coverage: tenant A cannot approve a tenant B workstation or Agent certificate, revoke/decommission it, or use it in a replacement operation; the foreign rows remain unchanged.
- Made both device-certificate enrollment activation and Agent-certificate proof atomic conditional database transitions. A proof only updates a workstation still in the expected active/enrolled state with the same bound thumbprint, so a late proof cannot overwrite concurrent revocation or rebinding; its audit row commits in the same transaction. A PostgreSQL test reads a stale enrollment, applies revocation, then proves the stale proof affects zero rows while a valid proof succeeds.
- Fixed a cross-tenant Monitoring disclosure: `AdminOnly` statistics and recent-authentication-event endpoints previously aggregated and returned all organizations' data, including user names and IP addresses. Both now require trusted organization context and constrain every query to that tenant; event projection also suppresses a user name if corrupt/cross-tenant user linkage is present. A→B coverage verifies counts, event visibility, mismatched user linkage, and fail-closed behavior without tenant context.
- Hardened audit, ANS-delegation, and pending PIN-reset projections against corrupt cross-tenant user foreign keys. Even in a SYSTEM_ADMIN query-filter-bypass context, names, account identifiers, and current RPPS are returned only when the linked user belongs to the log/request organization; orphaned foreign links become absent or `Unknown`. A regression test seeds Tenant A rows linked to Tenant B actor/target users and checks all three API projections.
- Audit target-name resolution now also verifies tenant ownership for both the referenced `UserPin` row and the user linked to a `Workflow`; a Tenant A audit cannot enrich itself with a Tenant B target through a corrupt foreign key.
- Workflow list, detail, and status mutation now fail closed when the workflow's owner user is absent from the workflow tenant. This prevents a malformed Tenant A workflow→Tenant B user link from disclosing the foreign user ID or allowing processing of an incorrectly owned workflow; the existing route-level A→B test now includes that corrupted-link case.
- Global settings updates now create a PostgreSQL durable operation intent and pending audit in one save before replacing the local JSON file. A startup recovery service replays pending operations before the API serves requests, atomically replaces the file, then completes the operation and audit together; completed rows clear the settings payload. A regression test forces file replacement to fail after intent commit, then recovers and verifies file state, operation state, and final audit. A host-unavailable content directory or database intentionally prevents readiness rather than serving unconfirmed settings.
- Tightened Vault and Kiosk app-session authorization against cross-tenant relational corruption and session confusion. Vault authorization now requires the loaded user and workstation to match the requested organization and an unended application session to match the exact user-session ID and application ID, not merely a matching app name. Kiosk app-session start likewise rejects user/workstation relations whose organization differs from the session. Regression tests exercise these paths under a privileged query-filter-bypass context.
- Kiosk badge/MIE identification now rejects an active device whose linked user belongs to another organization, even though credential lookup intentionally scans globally to detect duplicates. A malformed Tenant A device→Tenant B user foreign key is covered by a regression test and returns no identity.
- Hardened workstation session teardown to match both tenant and workstation for user, application, workstation, and Kiosk sessions, preventing malformed cross-tenant links from being ended by lifecycle operations. Added a regression test spanning all four session families. Heartbeat-created Credential Provider fleet rows now persist the organization from the authenticated workstation (also fixed the retired non-routable provider-health implementation). Fleet listing suppresses malformed foreign-tenant child data, and heartbeat rejects a foreign fleet/workstation binding rather than creating or updating a row; tests verify tenant persistence and no mutation on mismatch.
- Hardened the legacy fleet trust toggle: it can no longer reactivate pending or terminal (revoked/replaced/decommissioned) workstations outside the certificate-enrollment lifecycle. Administrators can still disable trust, and each successful trust change writes a tenant-bound audit record in the same database save. Tests cover pending/revoked denial, active enablement, allowed disablement, and audit ownership.
- Added Kiosk administrative A→B coverage for PIN reset, account status, unlock, badge assignment, and badge revocation. Foreign-user requests stop at the organization check without invoking the Kiosk service or modifying credential state.
- Added Kiosk session A→B coverage: a Kiosk from organization A cannot stop an application session or end a user session belonging to organization B; both are rejected before service invocation and the foreign sessions remain active.
- Hardened Kiosk PIN-reset requests: require organization context, same-tenant active target, Kiosk entitlement, and badge proof or self principal; if device-authenticated, verify its active workstation and derive audit/workflow machine identity from that workstation rather than request payload.
- Applied the signed `identity` module entitlement to every Devices/MIE read and write endpoint (including aggregate reads, device registration, status, certification, revocation, and statistics). Missing license/module fails closed with 403 before resource lookup or mutation; added denial tests for reads and writes while retaining tenant A→B coverage.
- Removed PIN-creation behavior that silently deleted an existing PIN row when a badge identifier collided. The flow now queries past tenant filters to honor the database-wide badge uniqueness constraint and rejects the operation without changing either tenant's credential; a SYSTEM_ADMIN-context A→B regression test covers the previously hidden cross-tenant deletion case.
- Applied the signed `identity` module entitlement to active Windows-auth enrollment, credential revocation, and re-enrollment mutations. Missing/invalid/revoked license or absent module returns 403 before workstation/user lookup or state changes; added endpoint-level denial coverage.
- Made Windows Auth enrollment require an `ACTIVE` workstation plus a same-tenant active, unlocked user; it now writes a tenant-bound `CredentialEnrolled` event in the same save as the enrollment fields. Tests cover foreign workstation/user, inactive and locked target denials with no event, and the valid local path.
- Removed the post-mutation audit gap in Agent Trust. Executable registration and Windows SID binding/revocation now save state and tenant/actor-bound audit rows atomically; SID values remain hashed. Tests verify foreign attempts create no audit, while own registration/bind/revoke produce correctly scoped audit records.
- Completed the `kiosk` module gate across active Kiosk operations, including admin PIN/badge/account changes, session stop/end, heartbeat, diagnostics, and status reads. Administrative status and reader telemetry are organization-scoped; the in-memory active-Kiosk registry is keyed by organization + workstation so identical hostnames cannot overwrite or reveal another tenant's device state. Added denial and A→B telemetry tests. Operations that return 501 without performing work remain disabled and unchanged.
- Extended the signed `identity` entitlement from user creation/import to directory search/read, update/delete, statistics, and both AD synchronization routes. Active directory/sync operations deny before queries or external LDAP calls when the module is unavailable; personal `/me` profile endpoints remain available to preserve self-service account recovery. Added route-level denial coverage.
- Applied the signed `identity` entitlement to policy list/create/update/delete, which previously relied on `AdminOnly` and tenant scoping but could still be used without an active licensed identity module. Denial occurs before policy reads or writes; a route-level test covers all four actions and confirms existing data remains unchanged.
- Added missing policy mutation audit coverage. Create/update/delete now add a redacted, tenant-bound record with authenticated actor, resource ID, and relevant old/new policy state in the same `SaveChanges` call as the mutation. Tests verify all three audit events and confirm foreign-tenant attempts neither mutate policy data nor generate an audit record.
- Made user update/delete audit rows commit atomically with owned-user mutations, and moved self-profile and single-user AD synchronization audits before the repository save so each audit persists with the corresponding mutation. Single-user AD import now explicitly initializes the user ID, safe default role, active state, and timestamps before inserting the audit, ensuring its resource ID points to the new user. Audit payloads identify changed field names and password-change status only; they do not copy email, phone, password, or directory attribute values. PostgreSQL regression tests cover AD update/create audit ownership and redaction, along with atomic user update/delete and self-profile audit behavior.
- License activation now writes its organization/actor-bound redacted audit row in the same `SaveChanges` operation as the signed entitlement update. A test verifies a valid Tenant A token cannot affect Tenant B, that a Tenant B token presented under Tenant A is rejected, and that signed license material is not stored in the audit.
- Instance-global Settings reads/writes and DB/LDAP/SMTP connectivity probes now fail closed with 409 when more than one organization shares a deployment. This prevents a tenant `SYSTEM_ADMIN` from reading or changing the host-wide JSON configuration or using connection probes in a multi-tenant context. License activation remains tenant-scoped and available. This deliberately does not invent a platform-admin feature; multi-tenant global configuration management remains an explicit product gap.
- Serialized tests that migrate or mutate the shared disposable PostgreSQL integration database in one xUnit collection; this prevents parallel schema-migration races observed during the expanded integration run while leaving unrelated test assemblies parallel.
- Applied the signed `kiosk` entitlement to the separate PIN-reset request/review controller as well as Kiosk endpoints; request, pending-list, approve, and reject all fail closed before DB access when unlicensed. Added denial tests confirming no request or authentication event is written.
- Made PIN-reset review a one-way `Pending` → `Approved`/`Rejected` decision. Relational providers perform a conditional state update inside the same transaction as `MustChangePin` and the authentication event, so racing approve/reject requests cannot both win or leave an unaudited state transition; non-relational unit-test providers retain an equivalent tracked-state check. Added terminal-state tests and a PostgreSQL race test asserting one winner and exactly one matching event.
- Bounded the tenant-scoped monitoring event query to 1–200 rows even when a caller supplies a negative or extremely large `limit`; the integration test seeds more than 200 records and verifies both boundaries while retaining cross-tenant suppression.
- Prevented integer overflow in user-directory paging by computing the offset as `long`, rejecting offsets beyond EF's supported `int` range before query execution, and passing the checked offset to `Skip`. Added coverage for `page=int.MaxValue` with the maximum page size.
- Clarified that device “certification” is only administrator-entered metadata: a true attestation now requires a trimmed reference of at most 256 non-control characters, the reference is cleared when the attestation is removed, and audit text states it is not externally verified by the API. The UI no longer labels these rows “PSI certified” or claims the API verified ANS status. Tests ensure empty/oversized references cause no state/audit mutation; frontend tests pass. This does not verify any real-world certificate.
- Fixed the standalone risk-policy evaluator's cross-tenant lookup: organization is part of the query key even under SYSTEM_ADMIN query-filter bypass, and mixed-organization user/workstation input is denied. Removed its unused always-true version-enforcement stub. Repository-wide search found no authentication pipeline invoking this evaluator, so policy enforcement remains a documented product gap and the UI now warns settings are not currently enforced.
- Unified application permission semantics across Kiosk session creation, Kiosk launch-list projection, Agent launch authorization, and Vault release: only non-expired explicit `use` or `launch` grants authorize use; read-only and unknown permission strings fail closed. A read-only permission previously allowed creating an app session that the Agent would later reject; a regression now proves the Kiosk denies before persisting the session or audit. The shared rule is documented in the authorization matrix.
- Removed two unregistered legacy API services that fabricated LDAP users, accepted badge updates unconditionally, and treated any PIN of sufficient length as valid. Runtime LDAP and Kiosk services remain the Core/Infrastructure implementations.
- Removed five retired Windows-auth actions that were only hidden by `[NonAction]` and retained tenant-unscoped validation or client-identity-based fleet mutation code. No repository callers existed; their route paths were already excluded from the generated inventory. The regression now asserts the methods do not exist; active enrollment/revoke routes remain, while unproven Windows session creation still fails with 501.
- Removed cross-tenant SignalR broadcasts: user-status and workflow/PIN-reset notifications now target an organization-specific administrator group, and unscoped/non-admin Hub identities receive no such group. Added tests proving tenant group separation.
- Made workflow creation and status transitions persist their audit records in the same database save as the workflow mutation. Audit details are limited to state/type metadata (never comments or form payloads); PostgreSQL-backed tests cover both operations, rollback-safe tenant isolation, and cross-tenant denial.
- Added an audit-staging API so Devices and workstation-enrollment mutations persist state plus redacted audit rows in the same `SaveChanges`; lifecycle workstation creation also remains inside its serializable transaction. New save-interceptor tests cover the four MIE mutation paths and five workstation transitions. Corrected MIE audit actor attribution to use the authenticated administrator rather than the target user whose credential was changed.
- Moved Kiosk PIN outcome, credential changes, user-session lifecycle, PIN-reset workflow, and application-session start/stop audits into the same save as their mutations. Successful portal login/logout now likewise commit session state, last-login time, and audit together. Save-interceptor tests assert same-save behavior, tenant/actor ownership, and PIN/badge redaction across these flows.
- Implemented an optional mTLS signed-revocation feed client and an atomic, sequence-monotonic local snapshot store. NexorSys Admin/feed-server integration remains external.
- Strengthened revocation snapshot monotonicity across independent API store instances by combining the in-process semaphore with an exclusive sidecar file handle across read/compare/atomic-replace. A concurrent two-store regression races adjacent signed sequence numbers over repeated rounds and confirms the final cache never rolls back. The lock is host-local; shared multi-host filesystem locking semantics and the authorized feed server remain deployment/integration validation.
- Added a clean-checkout verification runner and a deterministic ZIP construction path using `SOURCE_DATE_EPOCH` (or the source commit timestamp), sorted entries, and normalized ZIP timestamps.
- Made forwarded client-IP handling explicit and fail-closed for reverse-proxy deployments. `X-Forwarded-For`/`Proto` are processed only when exact `ReverseProxy:KnownProxies` IP literals are configured; default trusted networks are cleared, symmetry is required, and `ForwardLimit` is bounded to 1–5 (default 1). Middleware runs before HTTPS redirection and rate limiting. Tests verify disabled-by-default behavior, configured options, malformed configuration rejection, a trusted proxy changing client IP/scheme, and an untrusted peer's spoofed headers being ignored. Deployment instructions require protecting the API listener and validating the actual proxy chain. This verifies repository behavior only, not customer topology.
- Upgraded the GitHub Actions workflow so CI no longer stops at compilation: the Ubuntu job runs the backend security suite against its PostgreSQL service, verifies route-inventory freshness, runs frontend tests and production dependency audit; a separate Windows job runs the Agent and Kiosk suites, including the controlled signed-executable launch test. The CI token is restricted to `contents: read`. Both workflows pass local `actionlint` validation. Hosted CI has not yet run this revision, so no remote green-check claim is made.
- Vault credential release remains disabled.

## Verification observed during Phase 6

- Devices A→B coverage now exercises every DevicesController action under SYSTEM_ADMIN query-filter bypass and confirms no Tenant B data/credential disclosure or mutation. Latest integrated Release suite passed 168 tests (153 security, 4 Windows Agent, 11 Kiosk), 0 failed/skipped; strict Release solution build passed with 0 warnings/errors, and the 97-entry route inventory remains current. This is family-level concrete coverage and does not establish the still-open exhaustive authorization matrix for all route/resource/state combinations.
- After adding the workstation enrollment A→B read/replacement regressions, isolated synthetic clean snapshot `79af09cd5b3f` passed `scripts/verify-clean-checkout.ps1`: strict Release solution build (0 warnings/errors), 167 .NET tests (152 security, 4 Windows Agent, 11 Kiosk; 0 failures/skips), Kiosk Release build (0 warnings/errors), frontend clean install/tests (9/9)/production build (max chunk 283.45 kB), production npm audit (0 vulnerabilities), 97 route/action entries, and package manifest/checksum/artifact scans. Two independent packages from this snapshot were byte-identical, SHA-256 `B2290DA9A49D090E7BC37C37C91256022169BB1EDA893A6E898C37EE26784768`. The synthetic tree excluded Git metadata, generated/dependency directories, backups/scratch, local settings/secrets, personal signing containers and the legacy MSI; it does not establish canonical remote provenance. This evidence paragraph was added after package generation; the artifact contains the preceding report revision, with identical application and test source.
- Authorization A→B regression coverage now explicitly exercises foreign-workstation enrollment reads and cross-tenant replacement targets with SYSTEM_ADMIN global query filters bypassed; it asserts `404` and unchanged tenant A/B lifecycle state. Latest integrated Release suite passed 167 tests (152 security, 4 Windows Agent, 11 Kiosk), 0 failed/skipped; strict Release solution build passed with 0 warnings/errors, and generated route inventory remains current at 97 entries. This closes those specific authorization cases only, not the full endpoint/action/resource matrix.
- After Data Protection certificate rollover support was added, an isolated synthetic clean snapshot `8d5c2e16c61a` passed `scripts/verify-clean-checkout.ps1`: strict Release solution build (0 warnings/errors), 167 .NET tests (152 security, 4 Windows Agent, 11 Kiosk; 0 failures/skips), Kiosk Release build (0 warnings/errors), frontend 9/9, production dependency audit 0 vulnerabilities, largest JS chunk 283.45 kB, 97 generated routes, and package checksum/metadata/artifact scans. Two independent packages from this same clean synthetic commit were byte-identical, SHA-256 `A4FEA6489F7D839300F15D494E9A1FD8A4193374B078BB1BB8A44F194ED3F77B`. The snapshot excluded Git metadata, generated/dependency directories, backups/scratch, local settings/secrets, personal signing containers and legacy MSI, then received a synthetic local commit; this proves reproducibility for that snapshot, not canonical remote provenance. This evidence entry was added after packaging; the package contains the preceding report revision, while application/test/configuration content is identical.
- Data Protection rollover now uses `DataProtection:EncryptionCertificateThumbprint` for new key protection and optional `DataProtection:DecryptionCertificateThumbprints` for retained prior private keys; missing prior certificates/private keys fail production startup. Regression proves pre-rotation payloads and post-rotation payloads both decrypt after provider restart using the active-plus-previous certificate set. `scripts/test-all.ps1 -Configuration Release` passed 167 tests (152 security, 4 Windows Agent, 11 Kiosk), 0 failed/skipped; strict Release solution build passed with 0 warnings/errors and the route inventory remains current at 97 entries. This is software/deployment-rehearsal evidence only; customer certificate-store ACLs, service identity and rollout remain external host validation.
- Latest isolated synthetic snapshot `2cc248c5909a` includes the Agent end-session ownership and Vault-disabled regressions. `scripts/verify-clean-checkout.ps1` passed: strict Release solution build (0 warnings/errors), integrated tests 166 total (151 security, 4 Agent, 11 Kiosk; 0 failures/skips), Kiosk Release build, frontend 9/9, npm production audit 0 vulnerabilities, max JS chunk 283.45 kB, current 97-action inventory, and package metadata/checksum/artifact scans. Unsigned engineering ZIP SHA-256 `C7BE2A13EA9550AEBCDAB54B7D0D21B05C6E0A32293F8218B9FFE5379EF91E2E`; a second independent build from the same clean synthetic commit matched exactly. The snapshot excluded `.git`, generated/dependency directories, backups/scratch, local settings/secrets, personal signing containers and legacy MSI, then received a synthetic local commit; it does not establish canonical remote provenance. Only this evidence paragraph was written after packaging; the ZIP carries the preceding report revision, while application and test source are identical to the verified snapshot.
- Latest integrated Release run after adding the Agent end-session ownership and disabled-Vault controller regressions: `scripts/test-all.ps1 -Configuration Release` passed 166 tests (151 security, 4 Windows Agent, 11 Kiosk), 0 failed and 0 skipped. The end-session test checks tenant, workstation, certificate, SID, Windows session ID, and exact audit target while filters are bypassed; the Vault test proves both Agent routes remain closed with audited denials and no plaintext. Strict Release build succeeded with 0 warnings/errors; route inventory remains current at 97. These test/documentation-only additions postdate the clean package snapshot below; application source did not change.
- Latest isolated synthetic snapshot `fcb00c63e63d` includes the `/api/auth/me` claim-bound projection and added authorization regressions. The clean-checkout runner passed: strict Release build (0 warnings/errors), 164 .NET tests (149 security, 4 Agent, 11 Kiosk; 0 failures/skips), Kiosk Release build, frontend tests 9/9, production dependency audit 0 vulnerabilities, largest JS chunk 283.45 kB, 97 routes, and package checksum/metadata/artifact scans. Unsigned engineering artifact SHA-256: `EE7FCB9CDCE5634C0E2E02D895774114D2C1DBF3543DA0C7DC819B2D50C9C3F8`; an independent second build from the same clean synthetic commit produced the identical hash. The synthetic snapshot excludes Git metadata, generated/dependency directories, backups/scratch, local settings/secrets, personal signing containers, and old MSI; it does not establish provenance from the inaccessible authoritative remote. This report entry was added after package creation; the artifact includes the preceding report revision, while application, test, and authorization-matrix source were unchanged.
- Latest isolated synthetic source snapshot `2a6f02d0d63c` includes both background-worker corrections. `scripts/verify-clean-checkout.ps1` passed on that clean synthetic commit: strict Release build (0 warnings/errors), all 163 integrated tests (148 security, 4 Windows Agent, 11 Kiosk; 0 failed/skipped), Kiosk Release build, frontend clean install/audit (0 vulnerabilities), frontend tests (9/9), production bundle max 283.45 kB, 97-route generation, and release package checksum/metadata/artifact scan. Package SHA-256 was `3B21B6E7A856C8761379F918CEEF3FE1BC21BB7CAD0F00E352DBF9F4B82CE64B`; a second independent `build-release.ps1` run from the same clean snapshot produced the identical SHA-256. Both manifests report clean source, unsigned, and non-commercial. The snapshot was copied from the current worktree while excluding `.git`, generated/dependency directories, backups/scratch, local `.env`/settings, signing containers and old MSI, then assigned a new synthetic commit. This evidence paragraph was added to the report after packaging; application/test source was unchanged, but the artifact contains the preceding report revision. It is not a fresh clone or proof of provenance from the configured remote, which remains inaccessible; customer/host validation and commercial signing remain open.
- Latest integrated Release run after the background-worker, `/api/auth/me`, and Kiosk-identify coverage changes: `scripts/test-all.ps1 -Configuration Release` passed 164 tests (149 security, 4 Windows Agent, 11 Kiosk), with 0 failures and 0 skipped. PostgreSQL retention regression confirms cross-tenant deletion only for rows older than the configured cutoffs. Strict Release build (`--no-restore -warnaserror`) succeeded with 0 warnings/errors; route inventory remains current at 97 entries.
- After correcting background inactivity revocation and adding its no-HTTP-context, two-tenant ownership regression: `scripts/test-all.ps1 -Configuration Release` passed 162 tests (147 security, 4 Windows Agent, 11 Kiosk), 0 failed and 0 skipped. A strict Release solution build with `--no-restore -warnaserror` also succeeded with 0 warnings/errors. This source change has not yet been packaged or run through the clean synthetic checkout pipeline.
- Latest clean synthetic snapshot `014113c76793` passed `scripts/verify-clean-checkout.ps1` after Audit/Monitoring entitlement enforcement: strict Release build (0 warnings/errors), integrated suite (161 passed, 0 failed/skipped: 146 security, 4 Windows Agent, 11 Kiosk), Kiosk Release build, frontend clean install/audit (0 vulnerabilities), frontend tests (9/9), largest JS chunk 283.45 kB, current 97-route inventory, and package checksum/metadata/artifact-scan validation. Artifact `NexorSys-Identity-Kiosk-1.5.0-014113c76793.zip` SHA-256 `C3C3A77D2E2312F704DF9A6D238C916ED1B8358C84517424FB4241CA40E22FC8`; two sequential independent builds from the same snapshot yielded the identical hash. Metadata remains `sourceTreeClean: true`, `signed: false`, `commercialRelease: false`. This synthetic tree excludes Git metadata, generated build/dependency directories, SQL backups/scratch, local secret/config files, personal signing containers, and old MSI; it does not establish authoritative remote provenance. The report entry was added after packaging; no application/test source changed afterward.
- Latest integrated Release run after Audit/Monitoring entitlement enforcement: `scripts/test-all.ps1 -Configuration Release` passed 161 tests total (146 security, 4 Windows Agent, 11 Kiosk), zero failed/skipped. Denial coverage exercises audit history, monitoring statistics, and sign-in events without the signed `identity` module. Strict Release solution build with `--no-restore -warnaserror` succeeded with 0 warnings/errors, and route inventory remains current at 97 entries.
- Latest clean synthetic snapshot `0f5f8af5d60a` passed `scripts/verify-clean-checkout.ps1` after generic Workflow entitlement enforcement: strict Release solution build (0 warnings/errors), integrated .NET suite (160 passed, 0 failed/skipped: 145 security, 4 Windows Agent, 11 Kiosk), Kiosk Release build, frontend clean install/audit (0 vulnerabilities), frontend tests (9/9), production build (largest JS chunk 283.45 kB), 97-route generation, and package checksum/metadata/artifact-scan validation. Artifact `NexorSys-Identity-Kiosk-1.5.0-0f5f8af5d60a.zip` SHA-256 `F8E5D7281EBF7723B00B1D69C14CC9BA8DC8E0B7DD15CA15AC32A5555EDBB512`; two independent sequential builds from this exact synthetic snapshot produced the same SHA-256. Metadata confirms `sourceTreeClean: true`, `signed: false`, `commercialRelease: false`. As before, the snapshot excludes Git metadata, generated build/dependency directories, SQL backups/scratch, local secrets/configuration, personal signing containers and the old MSI, so it is not an authoritative remote clone. The configured origin still returns “Repository not found.” The package was built before this report-only entry was added; no application or test source changed after package generation.
- Latest integrated Release run after generic Workflow entitlement enforcement: `scripts/test-all.ps1 -Configuration Release` passed 160 tests total (145 security, 4 Windows Agent, 11 Kiosk), zero failed/skipped. All four generic Workflow actions deny with 403 before materializing or mutating data without the signed `identity` module. Strict Release solution build with `--no-restore -warnaserror` succeeded with 0 warnings/errors, and `Nexorsys.RouteInventory --check docs/api-route-inventory.json` confirms the 97 route/action entries remain current.
- Latest clean synthetic snapshot `8e33234aff16` passed `scripts/verify-clean-checkout.ps1` after ANS entitlement enforcement and before generic Workflow entitlement enforcement: strict Release solution build (0 warnings/errors), all integrated .NET tests (159 passed, 0 failed/skipped: 144 security, 4 Agent, 11 Kiosk), Kiosk Release build, frontend clean install/audit (0 vulnerabilities), frontend tests (9/9), production build (largest JS chunk 283.45 kB), 97-route inventory generation, and package metadata/checksum/artifact-scan validation. Package `NexorSys-Identity-Kiosk-1.5.0-8e33234aff16.zip` SHA-256 `728A80F002371832449437D7AF250EC7FD3B8B7A7469C887027C2F36E2E236DA`; two sequential independent builds from that exact snapshot produced the same SHA-256. Metadata confirms `sourceTreeClean: true`, `signed: false`, `commercialRelease: false`. The isolated snapshot was assembled from the current working source while excluding Git metadata, generated build/dependency directories, SQL backups/scratch, local secrets/configuration, personal signing containers, and the old MSI; it is synthetic and is not a clone of the authoritative remote. `git ls-remote origin HEAD` still returns “Repository not found.” The package predates the subsequent Workflow entitlement controller/test change and does not include that change.
- Latest integrated Release run after quota-conflict response handling: `scripts/test-all.ps1 -Configuration Release` passed 158 tests total (143 security, 4 Windows Agent, 11 Kiosk), zero failed/skipped. Strict Release solution build with `--no-restore -warnaserror` succeeded with 0 warnings/errors. The prior Linux and Windows targeted Data Protection results remain valid for the unchanged validator/test source. No clean package has been regenerated after the quota/API response change.
- Latest integrated Release run after Unix Data Protection key-file validation: `scripts/test-all.ps1 -Configuration Release` passed 156 tests total (141 security, 4 Windows Agent, 11 Kiosk), zero failed/skipped. The focused Data Protection suite passed 3/3 on Windows and 3/3 in Linux; the Linux execution exercised the new group-readable-file and symlink rejection paths. A subsequent host Release solution build with `--no-restore -warnaserror` succeeded with 0 warnings/errors. No package/clean-checkout artifact has been regenerated after this source change; the prior clean synthetic package below is evidence for its own earlier snapshot only.
- Latest clean synthetic snapshot `09f64e20a213` passed `scripts/verify-clean-checkout.ps1`: strict Release solution build (0 warnings/errors), PostgreSQL integrated tests (155 passed, 0 failed/skipped: 140 security, 4 Windows Agent, 11 Kiosk), Kiosk Release build, frontend clean install/audit (0 vulnerabilities), frontend tests (9/9), production build (largest JS chunk 283.45 kB), and 97-route inventory check. Artifact `NexorSys-Identity-Kiosk-1.5.0-09f64e20a213.zip` SHA-256 `198C0259FF3F4A1870D532D301AA8598F9AB784DE6544577C7CC4ECDB02E8ECB`; two independent builds from that exact snapshot produced the identical SHA-256. Metadata confirms `sourceTreeClean: true`, `signed: false`, `commercialRelease: false`. This isolated synthetic commit does not establish provenance from the authoritative remote or commercial readiness. The report was updated after packaging; no application or test source changed afterward.
- Latest integrated Release run after Agent Windows-session collision handling: 155 passed (140 security, 4 Windows Agent, 11 Kiosk), zero failed/skipped. A PostgreSQL barrier test makes two mTLS Agent bind requests observe no active row before insert; the partial unique index permits one binding and one creation-audit row, and the losing request receives `409 AGENT_WINDOWS_SESSION_ALREADY_BOUND` instead of an unhandled database error.
- Latest integrated Release run after handling concurrent Windows SID binding conflicts: 154 passed (139 security, 4 Windows Agent, 11 Kiosk), zero failed/skipped. A PostgreSQL barrier test forces two `AgentTrustController.BindWindowsIdentity` requests to observe no existing binding before either insert; the existing partial unique index permits exactly one active SID/workstation binding, the loser receives 409 rather than 500, and the database contains exactly one binding and matching audit row.
- Latest clean synthetic snapshot `b386a9920e42` passed `scripts/verify-clean-checkout.ps1`: strict Release build (0 warnings/errors), PostgreSQL integrated suite (154 passed, 0 failed/skipped: 139 security, 4 Agent, 11 Kiosk), Kiosk Release build (0 warnings/errors), frontend tests (9/9), dependency audit (0 vulnerabilities), production build (largest JS chunk 283.45 kB), 97-route inventory check, and package checksum/metadata validation. `NexorSys-Identity-Kiosk-1.5.0-b386a9920e42.zip` SHA-256 `76D7AC9957F5A6910B538F89A42B8A280CF8292D0CD003735F920AD1139B0BCD`; two independent rebuilds from this snapshot matched. Metadata confirms `sourceTreeClean: true`, `signed: false`, `commercialRelease: false`. This is a synthetic local commit and is not evidence of authoritative remote provenance. No application/test source changed after package generation; the report was updated afterward.
- Latest integrated Release run after adding active MIE credential uniqueness: 153 passed (138 security, 4 Windows Agent, 11 Kiosk), zero failed/skipped. PostgreSQL now enforces one non-revoked `(organization_id, device_type, device_identifier)` binding. The migration preflights existing collisions and aborts with operator guidance instead of deleting or selecting a credential owner. PostgreSQL tests assert the migrated partial unique index, race low-level inserts, and race the actual `DevicesController.RegisterDevice` path behind a barrier so both requests pass the precheck before save: one returns success, one returns 409, and exactly one device plus its matching audit row commits. The tests also confirm another tenant can independently use the identifier under current API semantics and that a revoked credential can be replaced.
- Latest clean synthetic snapshot `5775141fcc39` passed `scripts/verify-clean-checkout.ps1`: strict Release solution build (0 warnings/errors), PostgreSQL integrated tests (153 passed, 0 failed/skipped: 138 security, 4 Agent, 11 Kiosk), Kiosk Release build (0 warnings/errors), frontend clean install/audit (0 vulnerabilities), frontend tests (9/9), production bundle (largest JS chunk 283.45 kB), 97-route inventory check, and package checksum/metadata scan. Artifact `NexorSys-Identity-Kiosk-1.5.0-5775141fcc39.zip` SHA-256 `10C2AC6DAFAF169C28990906054602279A9C3787B8D830F597E5727A647AF895`; two independent rebuilds of the exact snapshot produced the same SHA-256. Metadata is explicitly `sourceTreeClean: true`, `signed: false`, `commercialRelease: false`. This isolated synthetic commit does not establish provenance from the authoritative remote. The report was updated after packaging; no application or test source changed afterward.
- Latest integrated Release run (`scripts/test-all.ps1 -Configuration Release`): 151 passed (136 security, 4 Agent, 11 Kiosk), zero failed/skipped. A ChangeTracker regression had shown that changing tenant A's MIE type could change a previously tracked tenant B primary device linked by user ID; `UpdateUser` now selects the primary device using explicit organization and user predicates. The Agent WTS test no longer assumes the current interactive session is unlocked; it now unit-tests the production parser with deterministic connected/unlocked, locked, disconnected, mismatched-session, invalid-session, and truncated-buffer cases.
- A new A→B regression exposed workstation hostname enrollment checking uniqueness globally when query filters are bypassed. Tenant B could block Tenant A from enrolling the same hostname, although the data model and unique index are tenant-scoped. `WorkstationEnrollmentController.Create` now checks the hostname with an explicit authenticated-organization predicate and uses that same organization for insertion. The regression simulates SYSTEM_ADMIN/global-filter bypass and verifies both organizations retain their own row. The pre-fix test failed with 409; the corrected focused test passes.
- Latest clean synthetic snapshot `51cebc776076` passed `scripts/verify-clean-checkout.ps1` after the hostname-scope fix: strict Release build (0 warnings/errors), integrated PostgreSQL Release suite (151 passed, 0 failed/skipped: 136 security, 4 Windows Agent, 11 Kiosk), Kiosk Release build, clean frontend install/audit (0 vulnerabilities), frontend tests (9/9), production build (largest JS chunk 283.45 kB), 97-route inventory check, and package checksum/metadata validation. Artifact `NexorSys-Identity-Kiosk-1.5.0-51cebc776076.zip` SHA-256 `182D029B6408B9509095649BD87D3C7359BB2283C83013BE313E525A748860FD`; two independent builds from that exact isolated source snapshot produced the same SHA-256. Metadata states `sourceTreeClean: true`, `signed: false`, `commercialRelease: false`. This was an isolated synthetic Git snapshot, not a fresh clone of the authoritative remote. The report was updated after packaging; no application or test source changed after verification.
- Latest integrated Release run (`scripts/test-all.ps1 -Configuration Release`) with disposable PostgreSQL: 150 passed, 0 failed, 0 skipped (135 security tests, including PostgreSQL; 4 Windows Agent; 11 Kiosk). Additional ChangeTracker-fixup regressions first reproduced cross-tenant permission disclosure (`tenant-b-secret-app` appeared in tenant A's user profile) and deletion of a tenant B permission during tenant A's permission update. Read projections now use no-tracking reads plus explicit tenant-filtered DTO projection; permission mutations load their targets with explicit tenant and user predicates and never infer mutation scope from a navigation collection. The clean synthetic pipeline passed: strict Release solution build (0 warnings/errors), all 150 .NET tests, Kiosk Release build, frontend clean install/audit (0 vulnerabilities), frontend tests (9/9), production build (largest JS chunk 283.45 kB), and 97-route inventory check. Package `NexorSys-Identity-Kiosk-1.5.0-1195e8ab8737.zip` SHA-256 `5959161BAFA118AE3904E980B6FE1177EDD13ADA0BC163040BD9786CF569EBAD`; two independent release builds matched. Metadata confirms `sourceTreeClean: true`, `signed: false`, `commercialRelease: false`. The report text was updated after packaging; no application/test source changed afterward. The synthetic commit does not establish authoritative remote provenance or commercial readiness.
- Latest integrated Release run (`scripts/test-all.ps1 -Configuration Release`) with disposable PostgreSQL: 147 passed, 0 failed, 0 skipped (132 security tests, including PostgreSQL; 4 Windows Agent; 11 Kiosk). Added `Local_user_creation_ignores_forged_tenant_role_and_foreign_application_assignments`, proving local user creation forces the authenticated tenant, viewer role and active/local state, and ignores an application belonging to another tenant. A clean synthetic snapshot of this exact source passed the complete pipeline: strict Release solution build (0 warnings/errors), all 147 .NET tests, Kiosk Release build, frontend install/audit (0 vulnerabilities), frontend tests (9/9), production build (largest JS chunk 283.45 kB), and 97-route inventory check. Package `NexorSys-Identity-Kiosk-1.5.0-c750d786b529.zip` SHA-256 `0691D9248AC9ADFD91D7274B550AB7E997A2297B9D50C39E415A3A6A8401540A`; two independent release builds reproduced the same SHA-256. Metadata confirms `sourceTreeClean: true`, `signed: false`, `commercialRelease: false`. This synthetic local commit does not establish authoritative remote provenance or commercial readiness.

- Latest integrated Release run (`scripts/test-all.ps1 -Configuration Release`) with disposable PostgreSQL: 141 passed, 0 failed, 0 skipped (126 security tests, including PostgreSQL, 4 Windows Agent tests, 11 Kiosk tests). New checks verify device routes opt out of JWT fallback only with the custom filter present, Production requires mTLS despite `[AllowAnonymous]`, authenticated `[Authorize]` admin/browser routes can pass the filter after authorization middleware, Development accepts only the configured shared key, invalid keys are denied, forwarded-header middleware honors a configured proxy while ignoring an untrusted peer's spoofed IP/protocol headers, racing PIN-reset decisions yield one terminal state and one matching event, and department-policy selection remains tenant scoped under query-filter bypass. Existing coverage continues to verify exact Kiosk↔Agent binary pins, tenant-header spoof resistance, Kiosk MIE and trace A→B enforcement, self-profile/Vault claim boundaries, privileged-context device statistics, Agent launch replay/audit-failure rollback, parallel PIN lockout, exact per-IP rate limits, and authorization/licensing/lifecycle/audit regressions.
- Focused Windows Kiosk test run after launch-flow expansion: 11 passed, 0 failed, 0 skipped. The controlled success and launcher-rejection tests require the signed .NET host at the standard Program Files path; they do not install or invoke a customer application.
- Frontend: 7/7 tests, production bundle largest JS chunk approximately 345.03 kB, and production dependency audit reported zero vulnerabilities.
- Frontend dashboard correction: removed the fabricated Kiosk-session trend, constant zero security score, and duplicate MIE/Kiosk count; the workflow chart now reflects the workflow API response, and partial API failures render unavailable values instead of false zero/disconnected states. Replaced Recharts with an accessible lightweight status bar chart and removed the unused dependency. Verification: 9/9 frontend tests, production audit 0 vulnerabilities, largest JavaScript chunk 283.45 kB (previously 345.03 kB; 17.8% smaller).
- Latest frontend verification after certification-attestation and policy-enforcement disclosures: tests 9/9, production build succeeds with largest JS chunk 283.45 kB, and `npm audit --omit=dev --audit-level=high` reports 0 vulnerabilities.
- Narrowed workflow-status mutation responses to a safe summary; comments and form payload are no longer serialized from the tracked entity. Audit API reads now redact sensitive key/value pairs at the response boundary, protecting legacy records that predate write-path redaction. Regression tests assert workflow responses exclude `Comments`/`FormData` and seeded legacy audit secrets are absent from API JSON. Integrated Release evidence at that earlier checkpoint: 130 passed, 0 failed, 0 skipped (115 security, 4 Agent, 11 Kiosk); see the latest verification section for current counts.
- Removed the constant Fleet heartbeat `latestConfigHash = "v2"`: no server-side configuration catalog or distributor exists in this repository, so heartbeat now reports no published hash and never echoes the client-supplied value. Added a test with a forged client hash to verify the response stays null while the tenant-bound heartbeat is persisted. The Fleet matrix documents this capability boundary.
- Secret-history scan using Gitleaks v8.30.1 covered 44 Git commits and reported 21 potential findings; a separate export-and-scan of committed `HEAD` reported 13 potential findings (JWT-shaped strings in test files and generic-key matches in configuration/settings history). A scan of the current modified worktree reported two matches exclusively in a pre-existing generated asset under the separate `frontend/nexorsys-identity-ui/dist` tree, not the maintained frontend source; whether that asset is deployed is unverified, and the matching bytes were not printed. Findings have not been individually confirmed as live credentials, rotated, or removed from Git history. Follow the rotation/incident process in `docs/phase5-security-operations.md`; credential owners must confirm and rotate live values, and any history rewrite requires repository-owner approval and coordinated fresh clones. Do not treat the scan as a clean secret-history result.
- Added `.github/workflows/secret-scan.yml` and `scripts/scan-secrets-git.ps1`: pinned Gitleaks v8.30.1 scans only incoming PR/push commit ranges, with scanner output redacted. A local no-change range passed; scanning the current `HEAD^..HEAD` found two generic-key matches in the committed SettingsController default/demo-license code. The current modified worktree removes those source lines, but the historical exposure remains in Git objects; treat associated values as potentially compromised until credential owners establish otherwise. Incremental CI prevention is not historical cleanup or proof of credential rotation.
- Latest clean synthetic snapshot `c5226e24b8ed` passed `scripts/verify-clean-checkout.ps1` on 2026-09-15 including the Agent certificate uniqueness migration, workstation-enrollment A→B regression, bounded revocation-feed reader, and no-certificate proof-route regression: strict .NET build (0 warnings/errors), integrated PostgreSQL Release tests (146 passed, 0 failed/skipped), Kiosk Release build, frontend clean install/audit (0 vulnerabilities), frontend tests (9/9), largest JavaScript chunk 283.45 kB, route inventory generation (97 actions), and unsigned package checksum/metadata verification. Two independent repeat builds of this snapshot produced byte-identical packages, SHA-256 `8F9900E6A2C1025505D872D8F4920494EE0E937693DB5F86F4B1704EFE2C9FE0`; metadata confirms `sourceTreeClean: true`, `signed: false`, `commercialRelease: false`. The isolated copy used a synthetic local Git commit and proves neither authoritative remote provenance nor commercial release readiness. The report was updated after package generation; no application/test source changed after verification.
- Superseded clean synthetic snapshot `f3fe9afa132d` passed the same pipeline with 145 tests and predates the anonymous workstation proof-controller regression; its repeatable artifact SHA-256 was `92C5A2AE8953D4CBDEFAFC0F746FC40FCD90994B481F7F89BA869BD04168EC51`.
- Superseded clean synthetic snapshot `009a950ce6f8` passed the same pipeline with 142 tests and predates the workstation-enrollment testability and A→B test addition; its repeatable artifact SHA-256 was `811FBE0DCD5B02C19B3E9DE44615EE5D0A0339BCAE5897C97511C3252E62C64F`.
- Superseded clean synthetic snapshot `2191438e0572` passed the same pipeline with 141 tests and prior Agent certificate index behavior; its repeatable artifact SHA-256 was `EA2DDB0ECBB6C27CEBB69CB3B380C896901B89246F2FB5BB0C2400BA4EEAAFAD`.
- After adding the Agent certificate uniqueness migration, the latest integrated Release run passed 142 tests (127 security tests, including PostgreSQL; 4 Windows Agent; 11 Kiosk), 0 failed and 0 skipped. The new PostgreSQL migration test passed against the applied schema. The full clean-checkout package pipeline has not yet been rerun on this later source state.
- Superseded synthetic snapshot `eb24817e5c04` passed the same verification with the prior application-permission behavior; its artifact SHA-256 was `86A2A3E210B8E8F99821E45DD341C45DC89CE8D3FE2A697C83209A1B329ECEA4`.
- Earlier clean synthetic snapshot `abb4a12` passed the same pipeline with 128 .NET tests and package SHA-256 `0C220C0761AD18B7EC374A82C3070E09F4FE42221DEC8590104995A66CF0C102`; it is superseded by the current snapshot above.
- Historical clean synthetic snapshot `f57b7e449651d82e892161fa269a791fe35ce3be` (before the exact IPC pinning change in this report) passed `scripts/verify-clean-checkout.ps1`: strict .NET build, integrated PostgreSQL suite (116 .NET tests, zero skipped), kiosk Release build, frontend clean install (0 production vulnerabilities), frontend tests (7/7), frontend production build, and unsigned engineering package checksum validation. It produced `NexorSys-Identity-Kiosk-1.5.0-f57b7e449651.zip` with SHA-256 `DF7C20E9706E2CE6BCB9470CED376D4F7295E6C58AF7CBDE1326D22E64497D1F`; package metadata reports `sourceTreeClean: true`, `signed: false`, `commercialRelease: false`. That artifact does not include the current IPC pinning change and is not evidence of a reproducible package for the current source. The synthetic tree also does not establish provenance from a fresh clone of the authoritative branch.

## Remaining software/security gaps

- Latest integrated Release run after monitoring hardening: 187 tests passed (172 security, 4 Windows Agent, 11 Kiosk), 0 failed, 0 skipped. Authentication-event `Reason` values are now redacted at the monitoring response boundary, with a regression proving a password-shaped value is not disclosed; tenant and user projection isolation remains covered.
- Latest integrated Release run after device-owner integrity hardening: 187 tests passed (172 security, 4 Windows Agent, 11 Kiosk), 0 failed, 0 skipped; strict Release build passed with 0 warnings/errors. Device detail and statistics queries now require both the device and its related user to belong to the authenticated organization, preventing malformed cross-tenant ownership rows from appearing in tenant reads or aggregates. The A→B regression now seeds such a malformed row and confirms it is excluded.
- Latest integrated Release run after Vault storage-boundary hardening: 188 tests passed (173 security, 4 Windows Agent, 11 Kiosk), 0 failed, 0 skipped; strict Release build passed with 0 warnings/errors. `VaultService.StoreAsync` now rejects an application, optional owner, or active actor that is not owned by the requested organization before encryption/persistence. This hardens storage ownership only; Agent grant/redeem remains `RELEASE_NOT_CONFIGURED` and Vault release stays disabled.
- Latest integrated Release run after PIN-reset response hardening: 188 tests passed (173 security, 4 Windows Agent, 11 Kiosk), 0 failed, 0 skipped; strict Release build passed with 0 warnings/errors. Pending PIN-reset `Reason` values are now redacted at the API response boundary, with the malformed-owner regression proving a password-shaped reason is not disclosed and no foreign request is decided or audited.

- The authorization review is not exhaustive. This checkpoint adds A→B regressions for forged tenant/role/application assignments during user creation, cross-tenant primary-device mutation, cross-tenant Windows-session revocation, and foreign-application permission exposure/removal under global-filter bypass. These tests cover concrete ownership cases, not the full systematic tenant/user/group/role/device/application/session/Vault/audit matrix. Some route families and lifecycle features are absent or partial; see `docs/phase6-product-capability-gaps.md`.
- Quota race coverage currently proves user and workstation limits with PostgreSQL serialization behavior. There is no independent device-seat or concurrent-session entitlement model to claim as enforced/tested.
- The launch flow has not been validated with the provisioned production Agent service, approved Kiosk/Agent publisher certificates, real mTLS certificates, an installed signed product, customer applications, Winlogon, or a customer workstation. Required machine trust configuration and ACLs remain deployment work; the implementation fails closed when trust material is absent.
- Data Protection startup now rejects directory/key-file reparse points and broad Windows ACL principals, requires direct Modify on the directory and Read on existing XML keys for the current service SID (with explicit recovery SID allowlisting), enforces Unix directory mode `0700`, checks Unix XML key-file permissions, and proves directory read/write access via a temporary probe. Production certificate rollover now supports an active encryption certificate plus retained decryption certificates; a restart regression covers payload continuity across rotation. Windows ACL and Linux Unix-mode/file/symlink tests have run. Actual installed service identity/certificate-store access, customer recovery-admin policy, multi-node use, inaccessible-directory deployment rehearsal, and customer recovery/rotation procedure remain host/customer validation.
- Revocation cache and protocol logic have tests, but no live authorized feed endpoint/server, client certificate deployment, shared multi-host cache coordination, or operations/alerting acceptance test exists.
- Resource ownership and audit behavior need a complete endpoint-by-endpoint review; this report does not claim that audit coverage is exhaustive.
- Endpoint-level audit/resource ownership review remains incomplete. Global settings recovery is durable for pending intents and verified by process-level failure simulation; production startup behavior with the installed host's database availability, ACLs, and content directory remains a deployment verification item.

## Release status and external dependencies

- Latest local verification: `scripts/test-all.ps1 -Configuration Release` passed 201 tests (186 security, 4 Windows Agent, 11 Kiosk), 0 failed, 0 skipped; strict Release solution build passed with 0 warnings/errors. The Kiosk response uses a dedicated DTO with no `Path` property, and regression coverage confirms local executable paths are absent from serialized Kiosk JSON.
- The release script was made runnable under the supported PowerShell runtime: it restores from a clean checkout, uses compatible random-byte generation, reads the npm lockfile through Node for SBOM generation, uses filesystem-relative paths for manifests/checksums/archive entries, and rejects traversal entries. Final local engineering packaging completed successfully with 97 route/action entries, frontend production build (largest JS chunk 283.31 kB), npm audit 0 vulnerabilities, and unsigned artifact SHA-256 `B06C277B76424CD812ABB4EA352E62BFC6BFC35F4FC1C175E205E62727C5860C`. The artifact was intentionally marked `sourceTreeClean: false`, `signed: false`, `commercialRelease: false` because the shared worktree is dirty; it is not a commercial release artifact. The package contains 218 valid entries, 217 checksum records, a manifest, and an SPDX SBOM; all embedded checksums match.

The package builder intentionally marks artifacts `signed: false` and `commercialRelease: false`; it excludes the legacy MSI. The synthetic clean-snapshot ZIP carries `sourceTreeClean: true`, passed all embedded checksum validations, and reproduced byte-for-byte on a second build; it does not prove provenance from the authoritative branch. No authorized commercial code-signing certificate/timestamp infrastructure is available here. Commercial MSI signing, installation validation, PKI/mTLS, AD/LDAPS, NFC/CPS/FIDO2 hardware, backup/restore in customer infrastructure, and independent penetration testing remain external validation items.

A current read-only `git ls-remote origin refs/heads/main` check returned “Repository not found,” so a fresh authoritative remote clone could not be verified from this environment. No remote write was attempted. The existing synthetic-snapshot evidence remains distinct from authoritative-branch provenance; the repository owner must provide a reachable canonical remote or validate the branch from their authorized environment.

Do not represent this result as “READY FOR EXTERNAL VALIDATION”: exhaustive authorization and A→B coverage, authoritative clean-clone provenance, and an observed run on the canonical remote remain incomplete. CI now contains the clean-checkout/package gate, but its execution against the authoritative remote still requires repository-host access. Commercial signing and customer-host integration are external prerequisites, not locally solvable software defects. Do not enable Vault release.
