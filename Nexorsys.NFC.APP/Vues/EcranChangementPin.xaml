<UserControl x:Class="Pinede.NFC.APP.Vues.EcranChangementPin"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800" 
             Background="{StaticResource BrushPrimarySage}">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border Style="{StaticResource GlassCard}" Width="500">
                <StackPanel>
                    <Image Source="/Assets/logo.png" MaxHeight="60" Margin="0,0,0,32" RenderOptions.BitmapScalingMode="HighQuality"/>
                    
                    <TextBlock Text="Sécurité de votre compte" Style="{StaticResource HeaderTitle}" FontSize="24" Margin="0,0,0,8"/>
                    <TextBlock Text="Vous devez changer votre code PIN temporaire" 
                               FontSize="16" Foreground="{StaticResource BrushTextDark}" 
                               HorizontalAlignment="Center" Margin="0,0,0,40"/>
                    
                    <TextBlock Text="Nouveau Code PIN :" 
                               FontSize="14" Foreground="{StaticResource BrushTextDark}" 
                               FontWeight="SemiBold" HorizontalAlignment="Left" Margin="0,0,0,8"/>
                    <PasswordBox Name="NewPinBox" Margin="0,0,0,20" Height="50" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="20" FontWeight="Bold"/>

                    <TextBlock Text="Confirmer le Code PIN :" 
                               FontSize="14" Foreground="{StaticResource BrushTextDark}" 
                               FontWeight="SemiBold" HorizontalAlignment="Left" Margin="0,0,0,8"/>
                    <PasswordBox Name="ConfirmPinBox" Margin="0,0,0,24" Height="50" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="20" FontWeight="Bold"/>
                    
                    <TextBlock Text="{Binding MessageErreur}" Foreground="#D64545" FontSize="13" FontWeight="Bold" 
                               Visibility="{Binding MessageErreur, Converter={StaticResource StringToVisibilityConverter}}"
                               HorizontalAlignment="Center" Margin="0,0,0,32" TextWrapping="Wrap" TextAlignment="Center"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="Annuler" Command="{Binding AnnulerCommand}" 
                                Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"/>
                        <Button Grid.Column="1" Content="Enregistrer et Continuer" Click="Enregistrer_Click" IsDefault="True"
                                Style="{StaticResource PrimaryButton}" Margin="8,0,0,0"
                                IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"/>
                    </Grid>
                </StackPanel>
            </Border>
            
            <TextBlock Text="Lead Architect : Ali HAMIDY" HorizontalAlignment="Center" Margin="0,32,0,0" Foreground="{StaticResource BrushClinicalGreen}" Opacity="0.5" FontSize="12" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</UserControl>
